<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Project form: smart button for all task equipment -->
    <record id="edit_project" model="ir.ui.view">
        <field name="name">project.project.form.inherit.oil_erp_equipment</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_equipment" type="object"
                        class="oe_stat_button" icon="fa-cogs"
                        invisible="not is_oil_gas_project">
                    <field name="equipment_count" widget="statinfo"
                           string="Equipment"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
