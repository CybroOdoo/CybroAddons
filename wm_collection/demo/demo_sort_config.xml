<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Demo sorting config for PET Plastic -->
        <record id="demo_sort_config_pet" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_pet_plastic')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_pet_plastic"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for PET plastic sorting</field>
        </record>

        <!-- Demo sorting config for Aluminum Cans -->
        <record id="demo_sort_config_aluminum" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_aluminium')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_aluminium"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Aluminum sorting</field>
        </record>

        <!-- Demo sorting config for Cardboard & Paper -->
        <record id="demo_sort_config_cardboard" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_cardboard')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_cardboard"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Cardboard and Paper shredding/baling</field>
        </record>

        <!-- Demo sorting config for Glass Bottles -->
        <record id="demo_sort_config_glass" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_glass')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_glass"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Glass sorting and crushing into cullet</field>
        </record>

        <!-- Demo sorting config for Steel Scrap -->
        <record id="demo_sort_config_steel" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_steel_scrap')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_steel_scrap"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Ferrous metal scrap magnetic separation</field>
        </record>

        <!-- Demo sorting config for Used Batteries -->
        <record id="demo_sort_config_batteries" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_batteries')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_batteries"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Battery chemical extraction &amp; metal recovery</field>
        </record>

        <!-- Demo sorting config for Printed Circuit Boards (PCBs) -->
        <record id="demo_sort_config_pcb" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_pcb')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_pcb"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for PCB hydrometallurgical precious metal recovery</field>
        </record>

        <!-- Demo sorting config for Cables & Electrical Wires -->
        <record id="demo_sort_config_cables" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_cables')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_cables"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Cable wire stripping and copper granulating</field>
        </record>

        <!-- Demo sorting config for Mobile Phones -->
        <record id="demo_sort_config_mobile_phones" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_mobile_phones')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_mobile_phones"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Mobile device disassembly &amp; component harvesting</field>
        </record>

        <!-- Demo sorting config for Used Motor Oil -->
        <record id="demo_sort_config_used_oil" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_used_oil')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_used_oil"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Waste oil re-refining and filtration</field>
        </record>

        <!-- Demo sorting config for Spent Solvents -->
        <record id="demo_sort_config_solvents" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_solvents')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_solvents"/>
            <field name="dest_location_id" ref="wm_collection.stock_location_landfill"/>
            <field name="notes">Demo configuration for Solvent thermal destruction / energy recovery</field>
        </record>

        <!-- Demo sorting config for Food Waste -->
        <record id="demo_sort_config_food_waste" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_food_waste')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_food_waste"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Food waste composting &amp; anaerobic digestion</field>
        </record>

        <!-- Demo sorting config for Garden Waste -->
        <record id="demo_sort_config_garden_waste" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_garden_waste')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_garden_waste"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Green waste shredding and windrow composting</field>
        </record>

        <!-- Demo sorting config for Concrete Rubble -->
        <record id="demo_sort_config_concrete" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_concrete_rubble')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_concrete_rubble"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Construction rubble crushing &amp; aggregate sorting</field>
        </record>

        <!-- Demo sorting config for Asbestos -->
        <record id="demo_sort_config_asbestos" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_asbestos')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_asbestos"/>
            <field name="dest_location_id" ref="wm_collection.stock_location_landfill"/>
            <field name="notes">Demo configuration for Asbestos controlled landfill cell containment</field>
        </record>

        <!-- Demo sorting config for Textile Waste -->
        <record id="demo_sort_config_textiles" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_textile_waste')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_textile_waste"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Fabric offcut shredding and fibre sorting</field>
        </record>

        <!-- Demo sorting config for Rubber & Tyre Waste -->
        <record id="demo_sort_config_rubber" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_rubber_waste')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_rubber_waste"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Tyre granulating and crumb rubber production</field>
        </record>

        <!-- Demo sorting config for Metal Swarf -->
        <record id="demo_sort_config_metal_shavings" model="wm.waste.material.sort.config">
            <field name="material_id" search="[('product_variant_ids', 'in', [ref('wm_base.wmat_metal_shavings')])]"/>
            <field name="dest_product_id" ref="wm_base.wmat_metal_shavings"/>
            <field name="dest_location_id" ref="stock.stock_location_stock"/>
            <field name="notes">Demo configuration for Machining swarf de-oiling and briquetting</field>
        </record>
    </data>
</odoo>
