<?xml version="1.0" encoding="UTF-8"?>
<!--This is the single template rendered by `OfflineSalesApp` (see the
    companion JS component). It implements a self-contained, offline-first
    point-of-sale UI with the following top-level regions-->
<templates xml:space="preserve">
    <t t-name="offline_sale.AppLayout" owl="1">
        <div class="h-screen w-screen flex flex-col bg-[#f7faf8] text-[#181c1b] font-body selection:bg-[#a0f2e1] selection:text-[#00201b] offline-sales-root">

            <!-- Back Online Sticky Toast -->
            <div t-if="state.showOnlineToast"
                 class="fixed top-6 right-6 z-[300] flex items-center gap-3 px-5 py-3 rounded-2xl shadow-2xl bg-white border-l-4 border-[#08c208] animate-in fade-in slide-in-from-right-8 duration-300 no-print">
                <div class="w-10 h-10 rounded-full bg-[#08c208]/15 flex items-center justify-center">
                    <span class="material-symbols-outlined text-[#08c208]" style="font-variation-settings: 'FILL' 1;">wifi</span>
                </div>
                <div class="pr-2">
                    <p class="font-headline font-bold text-sm text-[#08c208] uppercase tracking-tight">Back Online</p>
                    <p class="text-xs text-[#5c635f]">You are back to online</p>
                </div>
            </div>

            <!-- Gone Offline Sticky Toast -->
            <div t-if="state.showOfflineToast"
                 class="fixed top-6 right-6 z-[300] flex items-center gap-3 px-5 py-3 rounded-2xl shadow-2xl bg-white border-l-4 border-[#ba1a1a] animate-in fade-in slide-in-from-right-8 duration-300 no-print">
                <div class="w-10 h-10 rounded-full bg-[#ba1a1a]/15 flex items-center justify-center">
                    <span class="material-symbols-outlined text-[#ba1a1a]" style="font-variation-settings: 'FILL' 1;">wifi_off</span>
                </div>
                <div class="pr-2">
                    <p class="font-headline font-bold text-sm text-[#ba1a1a] uppercase tracking-tight">Offline</p>
                    <p class="text-xs text-[#5c635f]">You are now offline</p>
                </div>
            </div>

            <!-- Global Header -->
            <header class="flex justify-between items-center w-full px-6 py-3 h-16 bg-[#f7faf8] border-b border-[#e0e3e1] shrink-0 z-50 no-print">
                <div class="flex items-center gap-8">
                    <span class="text-xl font-black text-[#004f45] uppercase tracking-widest font-headline" t-esc="state.company.name"/>
                    <nav class="hidden md:flex gap-6">
                        <a href="#" t-on-click.prevent="() => state.screen = 'products'" 
                           t-att-class="state.screen === 'products' ? 'text-[#004f45] font-bold border-b-2 border-[#004f45] py-1' : 'text-[#5c635f] font-medium py-1 hover:text-[#004f45] transition-colors'"
                           class="font-headline tracking-tight">Terminal</a>
                    </nav>
                </div>
                <div class="flex items-center gap-4">
                    <div class="flex items-center gap-3 mr-4">
                        <div t-if="state.isServerDown" class="flex items-center gap-1.5 px-3 py-1 bg-[#ffdad6] text-[#ba1a1a] rounded-full text-[10px] font-bold animate-pulse">
                            <span class="material-symbols-outlined text-xs">error</span>
                            <span>SERVER STOPPED</span>
                        </div>
                        <div t-if="state.isSessionExpired" class="flex items-center gap-1.5 px-3 py-1 bg-[#fff4e5] text-[#b45309] rounded-full text-[10px] font-bold">
                            <span class="material-symbols-outlined text-xs">lock_clock</span>
                            <span>LOGIN EXPIRED</span>
                        </div>
                        <span class="material-symbols-outlined" t-att-class="state.isOnline ? 'text-[#08c208]' : 'text-red-500'">wifi</span>
                        <span class="material-symbols-outlined text-[#004f45]">battery_full</span>
                        <span class="material-symbols-outlined text-[#004f45] cursor-pointer" t-on-click="syncOrders">sync</span>
                    </div>
                    <button class="flex items-center gap-2 px-4 py-2 rounded-full bg-white border border-[#ba1a1a]/30 text-[#ba1a1a] text-xs font-bold uppercase tracking-wider hover:bg-[#ffdad6] active:scale-95 transition-all shadow-sm"
                            t-on-click="exitOfflineTerminal"
                            title="Exit Offline Terminal">
                        <span class="material-symbols-outlined text-base">logout</span>
                        <span class="hidden md:inline">Exit Terminal</span>
                    </button>
                    <div class="flex items-center gap-2 pl-4 border-l border-[#e0e3e1] cursor-pointer hover:opacity-80 transition-opacity" t-on-click="openEmployeeModal">
                        <div class="text-right hidden sm:block">
                            <p class="text-xs font-bold text-[#004f45]" t-esc="state.user_name"/>
                            <p class="text-[10px] text-[#5c635f]">User</p>
                        </div>
                        <div class="w-8 h-8 rounded-full bg-[#004f45] flex items-center justify-center text-white font-bold text-xs uppercase">
                            <t t-esc="state.user_name ? state.user_name[0] : 'U'"/>
                        </div>
                    </div>
                </div>
            </header>

            <div class="flex flex-1 overflow-hidden app-body-wrapper">
                <!-- Side Navigation -->
                <aside class="w-64 bg-[#ebefec] flex flex-col py-6 shrink-0 border-r border-[#e0e3e1] no-print">
                    <div class="px-8 mb-8">
                        <div class="flex items-center gap-3">
                            <div class="w-10 h-10 bg-[#004f45] rounded-xl flex items-center justify-center text-white">
                                <span class="material-symbols-outlined" style="font-variation-settings: 'FILL' 1;">architecture</span>
                            </div>
                            <div>
                                <h2 class="text-sm font-bold text-[#004f45] font-headline" t-esc="state.company.name"/>
                                <p class="text-[10px] text-[#5c635f] uppercase tracking-widest">Terminal 01</p>
                            </div>
                        </div>
                    </div>
                    <nav class="flex-1 flex flex-col font-medium text-sm">
                        <a href="#" t-on-click.prevent="() => state.screen = 'products'"
                           t-att-class="state.screen === 'products' ? 'bg-white text-[#004f45] shadow-sm ml-4 pl-4' : 'text-[#5c635f] px-8 hover:text-[#004f45]'"
                           class="rounded-l-full py-3 flex items-center gap-4 transition-all duration-200">
                            <span class="material-symbols-outlined">point_of_sale</span>
                            <span>Register</span>
                        </a>
                        <a href="#" t-on-click.prevent="openPendingOrders"
                           t-att-class="state.screen === 'pending_orders' ? 'bg-white text-[#004f45] shadow-sm ml-4 pl-4' : 'text-[#5c635f] px-8 hover:text-[#004f45]'"
                           class="rounded-l-full py-3 flex items-center gap-4 transition-all duration-200">
                            <span class="material-symbols-outlined">pending_actions</span>
                            <span>Orders</span>
                        </a>
                        <a href="#" t-on-click.prevent="() => state.screen = 'orders'"
                           t-att-class="state.screen === 'orders' ? 'bg-white text-[#004f45] shadow-sm ml-4 pl-4' : 'text-[#5c635f] px-8 hover:text-[#004f45]'"
                           class="rounded-l-full py-3 flex items-center gap-4 transition-all duration-200">
                            <span class="material-symbols-outlined">receipt_long</span>
                            <span>Transactions</span>
                        </a>
                        <div class="mt-8 px-8">
                            <button class="w-full bg-[#004f45] text-white py-3 rounded-xl flex items-center justify-center gap-2 font-bold shadow-lg shadow-[#004f45] hover:opacity-90 transition-all font-headline"
                                    t-on-click="newOrder">
                                <span class="material-symbols-outlined">add</span>
                                <span>New Sale</span>
                            </button>
                        </div>
                    </nav>
                </aside>

                <!-- MAIN WORKSPACE -->
                <main class="flex-1 overflow-hidden relative">
                    
                    <!-- PRODUCTS SCREEN -->
                    <t t-if="state.screen === 'products'">
                        <div class="flex h-full w-full">
                            <!-- Order Summary Column -->
                            <section class="w-[420px] bg-[#ebefec] flex flex-col shrink-0 border-r border-[#e0e3e1]">
                                <div class="flex-1 overflow-y-auto px-6 py-8 flex flex-col">
                                    <div class="flex justify-between items-baseline mb-6">
                                        <h3 class="text-[#181c1b] font-headline font-bold text-xl">Current Order</h3>
                                        <span class="text-xs text-[#5c635f] uppercase tracking-widest font-semibold" t-esc="state.cart.length + ' Items'"/>
                                    </div>

                                    <!-- Customer Selector in Summary -->
                                    <div class="mb-6 flex gap-2">
                                        <div class="relative group flex-1">
                                            <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-[#5c635f] text-sm">person</span>
                                            <select class="w-full bg-white border-0 rounded-xl h-11 pl-10 pr-4 text-sm font-medium focus:ring-2 focus:ring-[#004f45] appearance-none cursor-pointer"
                                                    t-model="state.currentPartner">
                                                <option value="">Select Customer...</option>
                                                <t t-foreach="state.partners" t-as="p" t-key="p.id">
                                                    <option t-att-value="p.id" t-esc="p.name"/>
                                                </t>
                                            </select>
                                        </div>
                                        <button class="w-11 h-11 bg-white hover:bg-[#004f45] hover:text-white text-[#5c635f] rounded-xl flex items-center justify-center transition-all shadow-sm group"
                                                title="Add New Customer"
                                                t-on-click="openCustomerForm">
                                            <span class="material-symbols-outlined text-lg">person_add</span>
                                        </button>
                                    </div>

                                    <!-- Cart Items -->
                                    <div class="space-y-4 flex-1">
                                        <t t-if="!state.cart.length">
                                            <div class="flex flex-col items-center justify-center py-20 opacity-20">
                                                <span class="material-symbols-outlined text-6xl">shopping_cart</span>
                                                <p class="font-bold mt-2">Empty Cart</p>
                                            </div>
                                        </t>
                                        <t t-foreach="state.cart" t-as="line" t-key="line.product_id">
                                            <div class="group flex items-center justify-between p-3 rounded-xl transition-all shadow-sm cursor-pointer"
                                                 t-on-click="() => this.selectLine(line.product_id)"
                                                 t-att-class="state.selectedLineId === line.product_id ? 'bg-[#004f45]/10 ring-2 ring-[#004f45]' : 'bg-white/50 hover:bg-white'">
                                                <div class="flex items-center gap-4">
                                                    <div class="w-12 h-12 rounded-lg bg-[#e0e3e1] flex items-center justify-center text-[#004f45]">
                                                        <span class="material-symbols-outlined">inventory_2</span>
                                                    </div>
                                                    <div>
                                                        <h4 class="text-[#181c1b] font-semibold text-sm line-clamp-1" t-esc="line.name"/>
                                                        <p class="text-[10px] text-[#5c635f] font-medium">
                                                            <t t-esc="line.qty.toFixed(2)"/> × <t t-esc="line.price_unit.toFixed(2)"/>
                                                            <t t-if="line.discount"> (-<t t-esc="line.discount.toFixed(2)"/>%)</t>
                                                            <t t-if="line.tax_ids &amp;&amp; line.tax_ids.length">
                                                                <span class="mx-1 opacity-50">•</span>
                                                                <t t-foreach="line.tax_ids" t-as="tid" t-key="tid">
                                                                    <span class="text-[9px] bg-[#004f45]/20 text-[#004f45] px-1.5 py-0.5 rounded-sm mr-1 font-bold" t-esc="getTaxName(tid)"/>
                                                                </t>
                                                            </t>
                                                        </p>
                                                    </div>
                                                </div>
                                                <div class="flex items-center gap-3">
                                                    <span class="text-[#181c1b] font-headline font-bold" t-esc="(line.qty * line.price_unit * (1 - (line.discount || 0)/100)).toFixed(2)"/>
                                                    <button class="text-red-400 hover:text-red-600 transition-colors" t-on-click.stop="() => this.removeFromCart(line.product_id)">
                                                        <span class="material-symbols-outlined text-sm">cancel</span>
                                                    </button>
                                                </div>
                                            </div>
                                        </t>
                                    </div>

                                    <!-- Totals -->
                                    <div class="mt-auto pt-6 border-t border-[#e0e3e1] space-y-2">
                                        <div class="flex justify-between text-[#5c635f]">
                                            <span class="text-sm font-medium">Subtotal</span>
                                            <span class="font-headline font-bold" t-esc="subtotal.toFixed(2)"/>
                                        </div>
                                        <t t-if="totalDiscount">
                                            <div class="flex justify-between text-emerald-600">
                                                <span class="text-sm font-medium">Discount</span>
                                                <span class="font-headline font-bold" t-esc="'-' + totalDiscount.toFixed(2)"/>
                                            </div>
                                        </t>
                                        <div class="flex justify-between text-[#5c635f]">
                                            <span class="text-sm font-medium">Taxes</span>
                                            <span class="font-headline font-bold" t-esc="tax.toFixed(2)"/>
                                        </div>
                                        <div class="flex justify-between items-end mt-4">
                                            <span class="text-lg font-bold">Total</span>
                                            <span class="text-4xl font-headline font-extrabold text-[#004f45] tracking-tight" t-esc="'$' + total.toFixed(2)"/>
                                        </div>
                                    </div>
                                </div>

                                <!-- Keypad Area -->
                                 <div class="bg-[#e5e9e6] p-6 flex flex-col gap-4">
                                    <div class="grid grid-cols-4 gap-2">
                                        <t t-foreach="['7','8','9','Qty','4','5','6','Disc','1','2','3','Price','Back','0','00','.','Clear','Barcode']" t-as="btn" t-key="btn">
                                            <button class="h-12 rounded-xl font-headline font-bold transition-all hover:bg-white flex items-center justify-center"
                                                    t-on-click="() => btn === 'Back' ? this.undo() : (btn === 'Barcode' ? this.toggleBarcodeInput() : this.handleKeypad(btn))"
                                                    t-att-class="['Qty','Disc','Price'].includes(btn) ? (state.keypadMode === btn.toLowerCase() ? 'bg-[#004f45] text-white shadow-inner' : 'bg-[#cfe6f2] text-[#004f45]') : (btn === 'Clear' ? 'bg-[#ffdad6] text-[#ba1a1a]' : (btn === 'Barcode' ? 'bg-[#004f45] text-white' : 'bg-[#e0e3e1] text-[#181c1b]'))">
                                                <t t-if="btn === 'Back'"><span class="material-symbols-outlined text-sm">undo</span></t>
                                                <t t-else=""><t t-esc="btn"/></t>
                                            </button>
                                        </t>
                                        <button class="bg-[#e0e3e1] text-[#181c1b] h-12 rounded-xl flex items-center justify-center hover:bg-white transition-all"
                                                t-on-click="() => this.handleKeypad('Backspace')">
                                            <span class="material-symbols-outlined text-sm">backspace</span>
                                        </button>
                                    </div>
                                    <div class="flex flex-col gap-3">
                                        <button class="w-full bg-white text-[#004f45] border-2 border-[#004f45] py-3 rounded-xl text-sm font-headline font-bold uppercase tracking-wider hover:bg-[#004f45]/5 transition-all"
                                                t-on-click="saveDraftOrder">
                                            Draft Order
                                        </button>
                                        <button class="w-full bg-gradient-to-r from-[#004f45] to-[#00695c] text-white py-3 rounded-xl text-sm font-headline font-bold uppercase tracking-wider shadow-lg shadow-[#004f45]/30 active:scale-[0.98] transition-all"
                                                t-on-click="confirmOrder">
                                            Confirm Order
                                        </button>
                                    </div>
                                </div>
                            </section>

                            <!-- Product Gallery Column -->
                            <section class="flex-1 bg-white flex flex-col p-8 overflow-hidden">
                                <div class="flex flex-col gap-6 mb-8 shrink-0">
                                    <div class="relative group">
                                        <span class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-[#5c635f] group-focus-within:text-[#004f45] transition-colors">search</span>
                                        <input type="text" placeholder="Search product gallery or SKU..."
                                               t-model="state.searchQuery"
                                               class="w-full bg-[#f7faf8] border-none rounded-full h-14 pl-12 pr-6 text-[#181c1b] placeholder:text-[#5c635f]/50 focus:ring-2 focus:ring-[#004f45]/20 shadow-sm transition-all"/>
                                    </div>

                                    <div class="flex items-center gap-3 overflow-x-auto pb-2 scrollbar-hide">
                                        <button class="px-6 py-2.5 rounded-full font-bold text-xs whitespace-nowrap transition-all shrink-0"
                                                t-on-click="() => state.selectedCategoryId = null"
                                                t-att-class="!state.selectedCategoryId ? 'bg-[#a0f2e1] text-[#00201b]' : 'bg-[#cfe6f2] text-[#004f45] hover:bg-[#b4cad6]'">
                                            All Collections
                                        </button>
                                        <t t-foreach="state.categories" t-as="cat" t-key="cat.id">
                                            <button class="px-6 py-2.5 rounded-full font-medium text-xs whitespace-nowrap transition-all shrink-0"
                                                    t-on-click="() => state.selectedCategoryId = cat.id"
                                                    t-att-class="state.selectedCategoryId === cat.id ? 'bg-[#a0f2e1] text-[#00201b] font-bold' : 'bg-[#cfe6f2] text-[#004f45] hover:bg-[#b4cad6]'">
                                                <t t-esc="cat.name"/>
                                            </button>
                                        </t>
                                    </div>
                                </div>

                                <div class="flex-1 overflow-y-auto pr-2 scrollbar-hide">
                                    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                                        <t t-foreach="filteredProducts" t-as="product" t-key="product.id">
                                            <div class="bg-white p-4 rounded-2xl flex flex-col gap-4 group cursor-pointer hover:shadow-2xl hover:shadow-black/5 transition-all active:scale-[0.98] border border-[#e0e3e1]/50"
                                                 t-on-click="() => this.addToCart(product)">
                                                <div class="aspect-square rounded-xl bg-[#f1f4f2] overflow-hidden relative flex items-center justify-center">
                                                    <img class="w-full h-full object-cover" 
                                                         t-attf-src="/web/image?model=product.product&amp;id={{product.id}}&amp;field=image_128"
                                                         loading="lazy"/>
                                                    <div class="absolute inset-0 bg-[#004f45]/0 group-hover:bg-[#004f45]/5 transition-colors"></div>
                                                </div>
                                                <div class="flex flex-col gap-1">
                                                    <h5 class="text-[#181c1b] font-bold text-sm line-clamp-1" t-esc="product.display_name"/>
                                                    <p class="text-[10px] text-[#5c635f] font-medium" t-esc="product.default_code or 'SKU'"/>
                                                    <p class="text-[#004f45] font-headline font-extrabold mt-1" t-esc="'$' + product.lst_price.toFixed(2)"/>
                                                </div>
                                            </div>
                                        </t>
                                    </div>
                                </div>
                            </section>
                        </div>
                    </t>

                    <!-- PAYMENT SCREEN -->
                    <t t-if="state.screen === 'payment'">
                        <div class="bg-[#d7dbd8] h-full flex flex-col overflow-hidden">
                            <div t-if="state.lastOrder and state.lastOrder.is_confirmed_backend_order"
                                 class="flex items-center gap-3 px-6 py-3 bg-gradient-to-r from-[#fff4e5] to-[#ffe6cc] border-b border-[#b45309]/30 no-print">
                                <span class="material-symbols-outlined text-[#b45309]" style="font-variation-settings: 'FILL' 1;">verified</span>
                                <div class="flex-1">
                                    <p class="text-sm font-headline font-bold text-[#7c2d12]">
                                        Order already confirmed in backend
                                        <span class="ml-2 text-xs font-bold uppercase tracking-widest bg-[#b45309] text-white px-2 py-0.5 rounded-full" t-esc="state.lastOrder.name"/>
                                    </p>
                                    <p class="text-xs text-[#7c2d12]/80">Invoice and payment are pending. Register the customer's payment to settle the balance due.</p>
                                </div>
                                <span class="text-xs font-bold uppercase tracking-widest text-[#b45309] bg-white border border-[#b45309]/40 px-3 py-1 rounded-full">
                                    Balance Due: <t t-esc="'$' + (state.lastOrder.amount_residual || state.lastOrder.amount_total || 0).toFixed(2)"/>
                                </span>
                            </div>
                            <main class="flex-1 grid grid-cols-12 overflow-hidden">
                                <!-- Column 1: Payment Methods -->
                                <section class="col-span-3 bg-[#ebefec] flex flex-col p-6 gap-6 border-r border-[#e0e3e1] overflow-y-auto">
                                    <div class="space-y-4">
                                        <div class="flex justify-between items-center">
                                            <h3 class="font-headline font-bold text-lg text-[#004f45]">Payment Terms</h3>
                                            <div class="flex gap-2">
                                                <button t-on-click="prevPaymentTerms" 
                                                        t-att-disabled="state.paymentTermPage === 0"
                                                        class="w-8 h-8 rounded-full bg-white flex items-center justify-center text-[#004f45] shadow-sm disabled:opacity-30 disabled:cursor-not-allowed hover:bg-[#ebefec] transition-all">
                                                    <span class="material-symbols-outlined text-sm">chevron_left</span>
                                                </button>
                                                <button t-on-click="nextPaymentTerms"
                                                        t-att-disabled="(state.paymentTermPage + 1) * 4 >= state.payment_terms.length"
                                                        class="w-8 h-8 rounded-full bg-white flex items-center justify-center text-[#004f45] shadow-sm disabled:opacity-30 disabled:cursor-not-allowed hover:bg-[#ebefec] transition-all">
                                                    <span class="material-symbols-outlined text-sm">chevron_right</span>
                                                </button>
                                            </div>
                                        </div>
                                        <div class="space-y-3">
                                            <t t-foreach="state.payment_terms.slice(state.paymentTermPage * 4, (state.paymentTermPage + 1) * 4)" t-as="pt" t-key="pt.id">
                                                <button class="w-full flex items-center gap-4 p-4 rounded-xl transition-all group"
                                                        t-on-click="() => state.selectedPaymentTermId = pt.id"
                                                        t-att-class="state.selectedPaymentTermId === pt.id ? 'bg-[#004f45] text-white shadow-lg ring-2 ring-[#004f45] ring-offset-2' : 'bg-white shadow-sm hover:ring-2 hover:ring-[#004f45]'">
                                                    <div class="w-12 h-12 rounded-full flex items-center justify-center shrink-0"
                                                         t-att-class="state.selectedPaymentTermId === pt.id ? 'bg-[#00695c] text-white' : 'bg-[#cfe6f2] text-[#004f45]'">
                                                        <span class="material-symbols-outlined text-2xl">list_alt</span>
                                                    </div>
                                                    <div class="text-left">
                                                        <p class="font-bold" t-esc="pt.name"/>
                                                        <p class="text-xs" t-att-class="state.selectedPaymentTermId === pt.id ? 'opacity-80' : 'text-[#5c635f]'">Billing Term</p>
                                                    </div>
                                                </button>
                                            </t>
                                        </div>
                                    </div>
                                    <div class="bg-[#f1f4f2] p-6 rounded-2xl space-y-3 mt-auto border border-[#004f45]/10">
                                        <div class="flex justify-between items-center text-[#5c635f] text-sm">
                                            <span>Subtotal</span>
                                            <span t-esc="(state.lastOrder.amount_untaxed + state.lastOrder.amount_discount).toFixed(2)"/>
                                        </div>
                                        <div class="flex justify-between items-center text-emerald-600 text-sm">
                                            <span>Discount</span>
                                            <span t-esc="'-' + state.lastOrder.amount_discount.toFixed(2)"/>
                                        </div>
                                        <div class="flex justify-between items-center text-[#5c635f] text-sm">
                                            <span>Taxes</span>
                                            <span t-esc="state.lastOrder.amount_tax.toFixed(2)"/>
                                        </div>
                                        <div class="pt-4 border-t border-[#e0e3e1] flex justify-between items-end">
                                            <span class="font-bold">Amount Due</span>
                                            <span class="font-headline font-bold text-2xl text-[#004f45]" t-esc="'$' + paymentDue.toFixed(2)"/>
                                        </div>
                                        <div t-if="state.paymentAmount > 0 and state.paymentAmount &lt;= paymentDue" class="flex justify-between items-baseline pt-2">
                                            <span class="text-xs font-bold text-[#5c635f] uppercase">Amount Paid</span>
                                            <span class="text-base font-headline font-bold text-[#08c208]" t-esc="'$' + Math.min(state.paymentAmount, paymentDue).toFixed(2)"/>
                                        </div>
                                        <div t-if="isPartialPayment" class="flex justify-between items-baseline pt-2 px-3 py-2 bg-[#fff4e5] rounded-xl border border-[#b45309]/30">
                                            <span class="text-xs font-bold text-[#b45309] uppercase">Balance Due</span>
                                            <span class="text-lg font-headline font-bold text-[#b45309]" t-esc="'$' + balanceDue.toFixed(2)"/>
                                        </div>
                                        <div t-if="state.paymentAmount > paymentDue" class="flex justify-between items-baseline pt-2">
                                            <span class="text-xs font-bold text-[#ba1a1a] uppercase">Change Balance</span>
                                            <span class="text-lg font-headline font-bold text-[#ba1a1a]" t-esc="'$' + (state.paymentAmount - paymentDue).toFixed(2)"/>
                                        </div>
                                        <div class="flex flex-col gap-3 mt-4">
                                            <button class="w-full py-4 rounded-full font-headline font-bold text-lg shadow-lg hover:opacity-90 active:scale-95 transition-all"
                                                    t-att-class="isPartialPayment ? 'bg-[#b45309] text-white shadow-[#b45309]/30' : 'bg-[#004f45] text-white'"
                                                    t-on-click="payNow">
                                                <t t-if="isPartialPayment">Validate Partial Payment</t>
                                                <t t-else="">Validate Payment</t>
                                            </button>
                                            <button class="w-full py-3 bg-white text-[#004f45] border border-[#004f45] rounded-full font-headline font-bold text-sm hover:bg-[#ebefec] transition-all"
                                                    t-on-click="backToProducts">
                                                Back to Order
                                            </button>
                                        </div>
                                    </div>
                                </section>

                                <!-- Column 2: Total & Keypad -->
                                 <section class="col-span-6 flex flex-col p-8 gap-8 bg-white border-r border-[#e0e3e1] overflow-y-auto">
                                    <div class="text-center py-6 space-y-2">
                                        <p class="text-xs text-[#5c635f] font-medium tracking-wide uppercase">Amount Tendered</p>
                                        <h1 class="font-headline font-extrabold text-[5rem] leading-none text-[#004f45] tracking-tighter" 
                                            t-esc="'$' + (state.paymentAmount || 0).toFixed(2)"/>
                                        <div t-if="state.paymentAmount > 0 and state.lastOrder and state.paymentAmount > paymentDue" class="pt-2">
                                            <p class="text-xs font-bold text-[#ba1a1a] uppercase">Remaining Change</p>
                                            <p class="text-2xl font-headline font-bold text-[#ba1a1a]"
                                               t-esc="'$' + (state.paymentAmount - paymentDue).toFixed(2)"/>
                                        </div>
                                        <div t-if="isPartialPayment" class="pt-2 inline-flex flex-col items-center mx-auto px-5 py-2 rounded-2xl bg-[#fff4e5] border border-[#b45309]/30">
                                            <p class="text-[10px] font-bold text-[#b45309] uppercase tracking-widest">Balance Due</p>
                                            <p class="text-3xl font-headline font-extrabold text-[#b45309] tracking-tight"
                                               t-esc="'$' + balanceDue.toFixed(2)"/>
                                            <p class="text-[10px] text-[#b45309]/70 italic">Customer is paying partially</p>
                                        </div>
                                    </div>
                                    <div class="max-w-md mx-auto w-full grid grid-cols-3 gap-3 pb-8">
                                        <t t-foreach="['1','2','3','4','5','6','7','8','9','+/-','0','.']" t-as="k" t-key="k">
                                            <button class="aspect-square flex items-center justify-center rounded-2xl font-headline text-2xl font-bold transition-all bg-[#e5e9e6] hover:bg-[#e0e3e1]"
                                                    t-on-click="() => this.handlePaymentKeypad(k)">
                                                <t t-esc="k"/>
                                            </button>
                                        </t>
                                        <button class="col-span-3 h-16 flex items-center justify-center rounded-2xl bg-[#ffdad6] text-[#ba1a1a] hover:bg-[#ba1a1a] hover:text-white transition-all font-bold"
                                                t-on-click="() => this.handlePaymentKeypad('Clear')">
                                            <span class="material-symbols-outlined mr-2">backspace</span>
                                            Clear Input
                                        </button>
                                    </div>
                                </section>

                                <!-- Column 3: Customer & Details -->
                                <section class="col-span-3 flex flex-col p-6 gap-6 bg-[#f7faf8] overflow-y-auto">
                                    <!-- Customer Card -->
                                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-[#e0e3e1]">
                                        <h4 class="font-headline font-bold mb-4">Customer</h4>
                                        <div class="flex items-center gap-4 mb-4">
                                            <div class="w-12 h-12 rounded-full bg-[#004f45] flex items-center justify-center text-white font-bold uppercase">
                                                <t t-esc="state.lastOrder.partner_name ? state.lastOrder.partner_name[0] : 'U'"/>
                                            </div>
                                            <div class="min-w-0">
                                                <p class="font-bold truncate" t-esc="state.lastOrder.partner_name"/>
                                                <p class="text-xs text-[#5c635f] truncate" t-esc="state.lastOrder.partner_city"/>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Backend Note Card (purple = backend identifier) -->
                                    <div class="bg-[#f5f3ff] p-5 rounded-2xl shadow-sm border border-[#7c3aed]/30">
                                        <div class="flex items-center justify-between mb-3">
                                            <div class="flex items-center gap-2">
                                                <span class="material-symbols-outlined text-[#7c3aed]" style="font-variation-settings: 'FILL' 1;">sticky_note_2</span>
                                                <h4 class="font-headline font-bold text-[#5b21b6]">Backend Note</h4>
                                            </div>
                                            <span class="text-[10px] font-bold uppercase tracking-widest text-[#7c3aed] bg-[#ede9fe] px-2 py-1 rounded-full">Internal</span>
                                        </div>
                                        <t t-if="state.backendNote">
                                            <p class="text-xs text-[#3b0764] whitespace-pre-line bg-white/70 p-3 rounded-xl border border-[#7c3aed]/20 mb-3 max-h-24" t-esc="state.backendNote"/>
                                        </t>
                                        <t t-else="">
                                            <p class="text-xs text-[#7c3aed]/70 italic mb-3">No internal note attached. Add one for backend identification.</p>
                                        </t>
                                        <div class="flex items-center gap-2">
                                            <button class="flex-1 py-2 px-3 rounded-xl bg-[#7c3aed] text-white text-xs font-bold uppercase tracking-wider hover:bg-[#6d28d9] active:scale-95 transition-all flex items-center justify-center gap-1"
                                                    t-on-click="openNoteModal">
                                                <span class="material-symbols-outlined text-sm">edit_note</span>
                                                <t t-if="state.backendNote">Edit Note</t>
                                                <t t-else="">Add Note</t>
                                            </button>
                                            <button t-if="state.backendNote"
                                                    class="py-2 px-3 rounded-xl bg-white border border-[#7c3aed]/30 text-[#7c3aed] text-xs font-bold hover:bg-[#ede9fe] transition-all"
                                                    t-on-click="clearBackendNote"
                                                    title="Clear note">
                                                <span class="material-symbols-outlined text-sm">delete</span>
                                            </button>
                                        </div>
                                    </div>

                                    <!-- Invoice Details -->
                                    <div class="flex-1 bg-white p-6 rounded-2xl shadow-sm border border-[#e0e3e1] flex flex-col overflow-hidden">
                                        <h4 class="font-headline font-bold mb-4">Invoice Details</h4>
                                        <div class="space-y-4 flex-1 pr-2 scrollbar-hide">
                                            <t t-foreach="state.lastOrder.lines" t-as="line" t-key="line.product_id">
                                                <div class="flex justify-between gap-4">
                                                    <div class="min-w-0">
                                                        <p class="text-sm font-bold truncate" t-esc="line.name"/>
                                                        <p class="text-xs text-[#5c635f]">Qty: <t t-esc="line.qty.toFixed(2)"/> x <t t-esc="line.price_unit.toFixed(2)"/></p>
                                                    </div>
                                                    <span class="font-headline font-bold" t-esc="(line.qty * line.price_unit).toFixed(2)"/>
                                                </div>
                                            </t>
                                        </div>
                                        <div class="pt-6 border-t border-[#e0e3e1] mt-auto">
                                            <div class="flex items-center gap-3">
                                                <button class="flex-1 py-3 px-4 border border-[#e0e3e1] rounded-full text-sm font-medium hover:bg-[#ebefec] transition-colors" t-on-click="printReceipt">
                                                    Print Preview
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </section>
                            </main>
                        </div>
                    </t>

                    <!-- RECEIPT SCREEN (Premium A4 Document View) -->
                    <t t-if="state.screen === 'receipt'">
                        <div class="h-full flex flex-col items-center bg-[#d7dbd8] overflow-y-auto w-full receipt-screen-wrapper">
                            <!-- Top Command Bar (Non-Printable) -->
                            <header class="w-full bg-white backdrop-blur-md z-50 no-print border-b border-[#bec9c5] shrink-0">
                                <div class="max-w-7xl mx-auto flex justify-between items-center px-6 py-4">
                                    <div class="flex items-center gap-3">
                                        <span class="font-headline font-extrabold text-[#004f45] tracking-tighter text-lg" t-esc="state.company.name"/>
                                        <div class="h-4 w-[1px] bg-[#bec9c5]/30"></div>
                                        <span class="font-headline font-bold text-[#004f45] text-sm opacity-80 uppercase tracking-widest">Document View</span>
                                    </div>
                                    <div class="flex items-center gap-4">
                                        <button class="flex items-center gap-2 px-4 py-2 rounded-full font-label text-sm text-[#3e4946] hover:bg-[#ebefec] transition-colors" t-on-click="newOrder">
                                            <span class="material-symbols-outlined text-[20px]">close</span>
                                            Exit View
                                        </button>
                                    </div>
                                </div>
                            </header>

                            <main class="w-full flex flex-col items-center py-8 relative receipt-main">
                                <!-- Action Cluster (Desktop Floating) -->
                                <div class="fixed right-10 top-1/2 -translate-y-1/2 flex flex-col gap-4 no-print hidden xl:flex z-50">
                                    <button class="w-14 h-14 rounded-full bg-[#004f45] text-white flex items-center justify-center shadow-lg hover:scale-110 active:scale-95 transition-all" t-on-click="printReceipt" title="Print Document">
                                        <span class="material-symbols-outlined">print</span>
                                    </button>
                                    <button class="w-14 h-14 rounded-full bg-white text-[#004f45] flex items-center justify-center shadow-lg hover:scale-110 active:scale-95 transition-all border border-[#bec9c5]" t-on-click="emailReceipt" title="Email PDF">
                                        <span class="material-symbols-outlined">mail</span>
                                    </button>
                                    <button class="w-14 h-14 rounded-full bg-white text-[#004f45] flex items-center justify-center shadow-lg hover:scale-110 active:scale-95 transition-all border border-[#bec9c5]" 
                                            t-on-click="downloadReceipt" title="Save as PDF">
                                        <span class="material-symbols-outlined">picture_as_pdf</span>
                                    </button>
                                </div>

                                <!-- A4 Document Content -->
                                <t t-foreach="lineChunks" t-as="chunk" t-key="chunk_index">
                                    <div class="a4-page relative flex flex-col text-left mb-8 shadow-2xl">
                                        <!-- BEGIN: Header Section -->
                                        <header class="flex justify-between items-start border-b-2 border-atelier-navy pb-6 mb-6 pt-2 shrink-0">
                                            <div data-purpose="company-info">
                                                <h1 class="text-2xl font-bold text-atelier-navy uppercase tracking-tight" t-esc="state.company.name"/>
                                                <div class="text-xs text-atelier-gray space-y-0.5 mt-1">
                                                    <p t-esc="state.company.street"/>
                                                    <p><t t-esc="state.company.city"/>, <t t-esc="state.company.zip"/></p>
                                                    <p>Phone: <t t-esc="state.company.phone"/></p>
                                                    <p>VAT ID: <t t-esc="state.company.vat or 'N/A'"/></p>
                                                </div>
                                            </div>
                                            <div class="text-right" data-purpose="transaction-meta">
                                                <h2 class="text-xl font-black text-atelier-teal uppercase leading-none mb-4">Transaction<br/>Receipt</h2>
                                                <div class="space-y-1">
                                                    <div class="flex justify-end gap-4">
                                                        <span class="label-text">Order Ref:</span>
                                                        <span class="value-text">#<t t-esc="state.lastOrder.name or state.lastOrder.uid"/></span>
                                                    </div>
                                                    <div class="flex justify-end gap-4">
                                                        <span class="label-text">Date:</span>
                                                        <span class="value-text" t-esc="state.lastOrder.date"/>
                                                    </div>
                                                    <div class="flex justify-end gap-4">
                                                        <span class="label-text">Status:</span>
                                                        <t t-if="state.lastOrder.is_partial">
                                                            <span class="value-text font-bold" style="color:#b45309;">PARTIAL</span>
                                                        </t>
                                                        <t t-else="">
                                                            <span class="value-text text-atelier-teal font-bold">PAID</span>
                                                        </t>
                                                    </div>
                                                    <div class="flex justify-end gap-4 text-[10px] text-gray-400">
                                                        <span>PAGE <t t-esc="chunk_index + 1"/> OF <t t-esc="lineChunks.length"/></span>
                                                    </div>
                                                </div>
                                            </div>
                                        </header>

                                        <!-- BEGIN: Customer & Payment Summary (Only on first page) -->
                                        <section t-if="chunk_index === 0" class="grid grid-cols-2 gap-4 mb-6 shrink-0">
                                            <div class="bg-atelier-light p-3 rounded border border-atelier-border" data-purpose="billed-to">
                                                <p class="label-text mb-1">Billed To</p>
                                                <p class="text-base font-bold text-atelier-navy" t-esc="state.lastOrder.partner_name"/>
                                                <span class="inline-block mt-1 px-2 py-0.5 bg-atelier-teal text-white text-[10px] font-bold rounded uppercase">
                                                    Platinum Tier Member
                                                </span>
                                            </div>
                                            <div class="bg-atelier-light p-3 rounded border border-atelier-border" data-purpose="payment-status">
                                                <p class="label-text mb-1">Payment Status</p>
                                                <t t-if="state.lastOrder.is_partial">
                                                    <p class="text-sm font-bold" style="color:#b45309;">PARTIAL — Balance Due</p>
                                                </t>
                                                <t t-else="">
                                                    <p class="text-sm font-semibold">Electronic Tender (Cash)</p>
                                                </t>
                                                <p class="text-[10px] text-gray-500 mt-1">TXID: <t t-esc="state.lastOrder.uid"/></p>
                                            </div>
                                        </section>

                                        <!-- BEGIN: Items Table -->
                                        <section class="">
                                            <table class="w-full text-left border-collapse">
                                                <thead>
                                                    <tr class="border-b border-atelier-navy">
                                                        <th class="py-2 label-text w-16">Qty</th>
                                                        <th class="py-2 label-text">Product Description</th>
                                                        <th class="py-2 label-text text-right w-24">Unit Price</th>
                                                        <th class="py-2 label-text text-right w-24">Total</th>
                                                    </tr>
                                                </thead>
                                                <tbody class="divide-y divide-atelier-border">
                                                    <t t-foreach="chunk" t-as="line" t-key="line.product_id">
                                                        <tr>
                                                            <td class="py-3 value-text align-top" t-esc="line.qty.toFixed(2)"/>
                                                            <td class="py-3">
                                                                <p class="value-text" t-esc="line.name"/>
                                                                <div class="flex items-center gap-2 mt-1">
                                                                    <span class="text-[10px] text-gray-500">SKU: ARCH-ITEM-<t t-esc="line.product_id"/></span>
                                                                    <t t-foreach="line.tax_ids" t-as="tid" t-key="tid">
                                                                        <span class="text-[9px] border border-gray-300 px-1 rounded uppercase"><t t-esc="getTaxName(tid)"/></span>
                                                                    </t>
                                                                </div>
                                                            </td>
                                                            <td class="py-3 value-text text-right align-top" t-esc="'$' + line.price_unit.toFixed(2)"/>
                                                            <td class="py-3 value-text text-right align-top" t-esc="'$' + (line.qty * line.price_unit).toFixed(2)"/>
                                                        </tr>
                                                    </t>
                                                </tbody>
                                            </table>
                                        </section>

                                        <!-- BEGIN: Totals Section (Only on last page) -->
                                        <section t-if="chunk_index === lineChunks.length - 1" class="pt-4 border-t border-atelier-border flex justify-between items-end shrink-0" style="margin-top: 1cm;">
                                            <div class="space-y-8 w-64" data-purpose="signatures">
                                                <div class="border-t border-atelier-border pt-1">
                                                    <p class="label-text">Authorized Signature</p>
                                                </div>
                                                <div class="border-t border-atelier-border pt-1">
                                                    <p class="label-text">Company Stamp</p>
                                                </div>
                                            </div>
                                            <div class="w-64 space-y-2">
                                                <div class="flex justify-between items-center text-sm">
                                                    <span class="text-gray-600">Subtotal</span>
                                                    <span class="font-semibold" t-esc="'$' + (state.lastOrder.amount_untaxed + state.lastOrder.amount_discount).toFixed(2)"/>
                                                </div>
                                                <t t-if="state.lastOrder.amount_discount">
                                                    <div class="flex justify-between items-center text-sm text-emerald-700">
                                                        <span class="opacity-80">Discount</span>
                                                        <span class="font-semibold" t-esc="'-$' + state.lastOrder.amount_discount.toFixed(2)"/>
                                                    </div>
                                                </t>
                                                <div class="flex justify-between items-center text-sm border-b border-atelier-border pb-2">
                                                    <span class="text-gray-600">Taxes</span>
                                                    <span class="font-semibold" t-esc="'$' + state.lastOrder.amount_tax.toFixed(2)"/>
                                                </div>
                                                <div class="flex justify-between items-center pt-1">
                                                    <span class="text-base font-bold text-atelier-navy uppercase">Grand Total</span>
                                                    <span class="text-2xl font-black text-atelier-teal" t-esc="'$' + state.lastOrder.amount_total.toFixed(2)"/>
                                                </div>
                                                <t t-if="state.lastOrder.amount_paid !== undefined">
                                                    <div class="flex justify-between items-center text-sm pt-2 border-t border-atelier-border">
                                                        <span class="text-gray-600">Amount Paid</span>
                                                        <span class="font-semibold" style="color:#08c208;" t-esc="'$' + (state.lastOrder.amount_paid || 0).toFixed(2)"/>
                                                    </div>
                                                </t>
                                                <t t-if="state.lastOrder.is_partial">
                                                    <div class="flex justify-between items-center text-sm px-2 py-2 rounded" style="background-color:#fff4e5; border:1px solid rgba(180,83,9,0.3);">
                                                        <span class="font-bold uppercase tracking-wider text-[10px]" style="color:#b45309;">Balance Due</span>
                                                        <span class="font-black text-base" style="color:#b45309;" t-esc="'$' + (state.lastOrder.amount_residual || 0).toFixed(2)"/>
                                                    </div>
                                                </t>
                                                <div t-if="state.paymentAmount > paymentDue" class="flex justify-between items-center text-xs text-gray-500 italic">
                                                    <span>Change Balance</span>
                                                    <span t-esc="'$' + (state.paymentAmount - paymentDue).toFixed(2)"/>
                                                </div>
                                            </div>
                                        </section>

                                        <!-- BEGIN: Footer and Signatures (Only on last page) -->
                                        <footer t-if="chunk_index === lineChunks.length - 1" class="mt-auto pt-8 shrink-0">
                                            <div class="text-center border-t border-atelier-border pt-4">
                                                <p class="text-[8px] text-gray-400 uppercase tracking-[0.3em]">
                                                    Generated via APEX Ledger Cloud POS • Document UID: <t t-esc="state.lastOrder.uid"/>
                                                </p>
                                            </div>
                                        </footer>
                                    </div>
                                </t>

                                <!-- Mobile Action Bar -->
                                <div class="mt-8 flex flex-col md:flex-row gap-4 items-center justify-center no-print pb-12 xl:hidden">
                                    <button class="flex items-center gap-2 px-8 py-4 bg-[#004f45] text-white rounded-full font-headline font-bold text-sm tracking-tight shadow-lg w-64 justify-center" t-on-click="printReceipt">
                                        <span class="material-symbols-outlined">print</span>
                                        Print Physical Copy
                                    </button>
                                    <button class="flex items-center gap-2 px-8 py-4 bg-white text-[#004f45] border border-[#bec9c5] rounded-full font-headline font-bold text-sm tracking-tight shadow-md w-64 justify-center">
                                        <span class="material-symbols-outlined">alternate_email</span>
                                        Email to Customer
                                    </button>
                                </div>
                            </main>
                        </div>
                    </t>

                    <!-- PENDING (BACKEND) ORDERS SCREEN -->
                    <t t-if="state.screen === 'pending_orders'">
                        <div class="h-full flex flex-col bg-[#f7faf8] p-10">
                            <div class="flex justify-between items-center mb-8">
                                <div>
                                    <h2 class="text-3xl font-headline font-extrabold text-[#004f45]">Pending Orders</h2>
                                    <p class="text-xs text-[#5c635f] mt-1">Backend sale orders that are not cancelled and still have a balance due.</p>
                                </div>
                                <button class="bg-white px-6 py-3 rounded-xl border border-[#e0e3e1] shadow-sm font-bold flex items-center gap-2"
                                        t-on-click="loadPendingOrders">
                                    <span class="material-symbols-outlined" t-att-class="state.pendingOrdersLoading ? 'animate-spin' : ''">refresh</span>
                                    Refresh
                                </button>
                            </div>

                            <div class="flex-1 overflow-y-auto rounded-3xl border border-[#e0e3e1] bg-white shadow-xl">
                                <t t-if="state.pendingOrdersLoading">
                                    <div class="flex items-center justify-center py-20 text-[#5c635f]">
                                        <span class="material-symbols-outlined animate-spin mr-2">refresh</span>
                                        Loading pending orders...
                                    </div>
                                </t>
                                <t t-elif="!state.pendingOrders.length">
                                    <div class="flex flex-col items-center justify-center py-20 text-[#5c635f]">
                                        <span class="material-symbols-outlined text-6xl opacity-30">inbox</span>
                                        <p class="mt-3 font-bold">No pending orders</p>
                                        <p class="text-xs">All backend orders have been completed.</p>
                                    </div>
                                </t>
                                <t t-else="">
                                    <table class="w-full border-collapse">
                                        <thead class="sticky top-0 bg-[#ebefec] text-left text-xs uppercase tracking-widest text-[#5c635f]">
                                            <tr>
                                                <th class="p-6">Reference</th>
                                                <th class="p-6">Customer</th>
                                                <th class="p-6">Date</th>
                                                <th class="p-6">Total</th>
                                                <th class="p-6">Balance Due</th>
                                                <th class="p-6">Status</th>
                                                <th class="p-6 text-right">Action</th>
                                            </tr>
                                        </thead>
                                        <tbody class="divide-y divide-[#e0e3e1]">
                                            <t t-foreach="state.pendingOrders" t-as="po" t-key="po.id">
                                                <t t-set="processed" t-value="isPendingOrderProcessed(po.id)"/>
                                                <tr class="hover:bg-[#f7faf8] transition-colors" t-att-class="processed ? 'opacity-60' : ''">
                                                    <td class="p-6 font-bold text-[#004f45]" t-esc="po.name"/>
                                                    <td class="p-6 font-medium" t-esc="po.partner_name"/>
                                                    <td class="p-6 text-[#5c635f]" t-esc="po.date"/>
                                                    <td class="p-6 font-headline font-bold" t-esc="'$' + (po.amount_total || 0).toFixed(2)"/>
                                                    <td class="p-6 font-headline font-bold text-[#b45309]" t-esc="'$' + (po.amount_residual || 0).toFixed(2)"/>
                                                    <td class="p-6">
                                                        <div class="flex flex-col gap-1">
                                                            <span t-if="po.state === 'draft'" class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[10px] font-bold uppercase tracking-wider text-center">Draft</span>
                                                            <span t-elif="po.state === 'sent'" class="px-3 py-1 bg-cyan-100 text-cyan-700 rounded-full text-[10px] font-bold uppercase tracking-wider text-center">Sent</span>
                                                            <span t-else="" class="px-3 py-1 bg-purple-100 text-purple-700 rounded-full text-[10px] font-bold uppercase tracking-wider text-center" t-esc="po.state"/>
                                                            <span class="px-3 py-1 bg-orange-100 text-orange-700 rounded-full text-[10px] font-bold uppercase tracking-wider text-center" t-esc="po.invoice_status"/>
                                                            <span t-if="processed" class="px-3 py-1 bg-amber-100 text-amber-800 rounded-full text-[10px] font-bold uppercase tracking-wider text-center">Paid · Pending Sync</span>
                                                        </div>
                                                    </td>
                                                    <td class="p-6 text-right">
                                                        <t t-if="processed">
                                                            <span class="inline-flex items-center gap-1 text-[11px] font-bold uppercase tracking-wider text-[#b45309]">
                                                                <span class="material-symbols-outlined text-base">cloud_sync</span>
                                                                Awaiting Sync
                                                            </span>
                                                        </t>
                                                        <t t-else="">
                                                            <button class="bg-[#004f45] text-white px-4 py-2 rounded-lg text-[11px] font-bold uppercase hover:opacity-90 active:scale-95 transition-all inline-flex items-center gap-1"
                                                                    t-on-click="() => this.loadPendingOrder(po)">
                                                                <span class="material-symbols-outlined text-sm">download</span>
                                                                Load Order
                                                            </button>
                                                        </t>
                                                    </td>
                                                </tr>
                                            </t>
                                        </tbody>
                                    </table>
                                </t>
                            </div>
                        </div>
                    </t>

                    <!-- ORDERS LIST SCREEN -->
                    <t t-if="state.screen === 'orders'">
                        <div class="h-full flex flex-col bg-[#f7faf8] p-10">
                            <div class="flex justify-between items-center mb-8">
                                <h2 class="text-3xl font-headline font-extrabold text-[#004f45]">Transaction History</h2>
                                <button class="bg-white px-6 py-3 rounded-xl border border-[#e0e3e1] shadow-sm font-bold flex items-center gap-2"
                                        t-on-click="() => this.syncOrders(false, true)">
                                    <span class="material-symbols-outlined" t-att-class="state.syncStatus === 'syncing' ? 'animate-spin' : ''">sync</span>
                                    Sync Orders
                                </button>
                            </div>

                            <div class="flex-1 overflow-y-auto rounded-3xl border border-[#e0e3e1] bg-white shadow-xl">
                                <table class="w-full border-collapse">
                                    <thead class="sticky top-0 bg-[#ebefec] text-left text-xs uppercase tracking-widest text-[#5c635f]">
                                        <tr>
                                            <th class="p-6">Reference</th>
                                            <th class="p-6">Customer</th>
                                            <th class="p-6">Date</th>
                                            <th class="p-6">Amount</th>
                                            <th class="p-6">Status</th>
                                        </tr>
                                    </thead>
                                    <tbody class="divide-y divide-[#e0e3e1]">
                                        <t t-foreach="state.syncedOrders" t-as="order" t-key="order.uid">
                                            <tr class="hover:bg-[#f7faf8] transition-colors">
                                                <td class="p-6 font-bold text-[#004f45]" t-esc="order.name or order.uid"/>
                                                <td class="p-6 font-medium" t-esc="order.partner_name"/>
                                                <td class="p-6 text-[#5c635f]" t-esc="order.date"/>
                                                <td class="p-6 font-headline font-bold" t-esc="'$' + order.amount_total.toFixed(2)"/>
                                                <td class="p-6">
                                                    <div class="flex items-center gap-3">
                                                        <div class="flex flex-col gap-1">
                                                            <span t-if="order.is_synced" class="px-3 py-1 bg-green-100 text-green-700 rounded-full text-[10px] font-bold uppercase tracking-wider text-center">Synced</span>
                                                            <span t-else="" class="px-3 py-1 bg-orange-100 text-orange-700 rounded-full text-[10px] font-bold uppercase tracking-wider text-center">Pending</span>
                                                            <span t-if="order.state === 'draft'" class="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-[10px] font-bold uppercase tracking-wider text-center">Draft</span>
                                                            <span t-else="" class="px-3 py-1 bg-purple-100 text-purple-700 rounded-full text-[10px] font-bold uppercase tracking-wider text-center">Confirmed</span>
                                                        </div>
                                                        <button t-if="order.state === 'draft'" 
                                                                class="bg-[#004f45] text-white px-4 py-2 rounded-lg text-[10px] font-bold uppercase hover:opacity-90"
                                                                t-on-click="() => this.loadOrder(order)">
                                                            Load Order
                                                        </button>
                                                        <button t-if="!order.is_synced" 
                                                                class="text-[#ba1a1a] hover:bg-[#ffdad6] p-2 rounded-full transition-colors flex items-center justify-center ml-2"
                                                                t-on-click="() => this.deleteOrder(order.uid)"
                                                                title="Delete Order">
                                                            <span class="material-symbols-outlined text-sm">delete</span>
                                                        </button>
                                                    </div>
                                                </td>
                                            </tr>
                                        </t>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </t>

                </main>

                <!-- Screen 5: Create Customer Form -->
                <div t-if="state.screen === 'customer_form'" class="fixed inset-0 z-[100] bg-[#f7faf8] flex flex-col items-center justify-center p-4 md:p-8 animate-in fade-in zoom-in duration-300 no-print">
                    <main class="w-full max-w-4xl bg-white rounded-2xl shadow-2xl border border-[#ebefec] overflow-hidden flex flex-col">
                        <div class="p-8 md:p-12 overflow-y-auto max-h-[90vh] scrollbar-hide">
                            <div class="mb-12">
                                <div class="space-y-6">
                                    <div class="relative group">
                                        <input class="w-full text-4xl md:text-5xl font-manrope font-bold bg-transparent border-none p-0 focus:ring-0 placeholder:text-[#e0e3e1] text-[#181c1b]" 
                                               placeholder="Customer Name" type="text"
                                               t-model="state.customerForm.name"/>
                                        <div class="h-0.5 w-0 group-focus-within:w-full bg-[#00695c] transition-all duration-500"></div>
                                    </div>
                                </div>
                            </div>

                            <div class="grid grid-cols-1 md:grid-cols-2 gap-12 md:gap-16">
                                <!-- Left Column: Address -->
                                <div class="space-y-8">
                                    <div class="space-y-6">
                                        <h3 class="text-xs font-bold uppercase tracking-[0.2em] text-[#6e7976] border-b border-[#bec9c5] pb-2">Address Details</h3>
                                        <div class="space-y-4">
                                            <div class="group">
                                                <label class="block text-[10px] font-bold uppercase text-[#6e7976] mb-1">Street Address</label>
                                                <input class="w-full bg-[#f1f4f2] border-none rounded-xl px-4 py-3 focus:ring-2 focus:ring-[#00695c] transition-all" 
                                                       placeholder="123 Industrial Way" type="text" t-model="state.customerForm.street"/>
                                            </div>
                                            <div class="grid grid-cols-2 gap-4">
                                                <div class="group">
                                                    <label class="block text-[10px] font-bold uppercase text-[#6e7976] mb-1">City</label>
                                                    <input class="w-full bg-[#f1f4f2] border-none rounded-xl px-4 py-3 focus:ring-2 focus:ring-[#00695c] transition-all" 
                                                           placeholder="Portland" type="text" t-model="state.customerForm.city"/>
                                                </div>
                                                <div class="group">
                                                    <label class="block text-[10px] font-bold uppercase text-[#6e7976] mb-1">ZIP / Postal Code</label>
                                                    <input class="w-full bg-[#f1f4f2] border-none rounded-xl px-4 py-3 focus:ring-2 focus:ring-[#00695c] transition-all" 
                                                           placeholder="97201" type="text" t-model="state.customerForm.zip"/>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="group">
                                        <label class="block text-[10px] font-bold uppercase text-[#6e7976] mb-1">Tax ID (VAT)</label>
                                        <input class="w-full bg-[#f1f4f2] border-none rounded-xl px-4 py-3 focus:ring-2 focus:ring-[#00695c] transition-all font-mono" 
                                               placeholder="XX-XXXXXXX" type="text" t-model="state.customerForm.vat"/>
                                    </div>
                                </div>

                                <!-- Right Column: Communication -->
                                <div class="space-y-8">
                                    <div class="space-y-6">
                                        <h3 class="text-xs font-bold uppercase tracking-[0.2em] text-[#6e7976] border-b border-[#bec9c5] pb-2">Communication</h3>
                                        <div class="space-y-4">
                                            <div class="group">
                                                <label class="block text-[10px] font-bold uppercase text-[#6e7976] mb-1">Phone / Mobile</label>
                                                <input class="w-full bg-[#f1f4f2] border-none rounded-xl px-4 py-3 focus:ring-2 focus:ring-[#00695c] transition-all" 
                                                       placeholder="+1 (503) 555-0199" type="tel" t-model="state.customerForm.phone"/>
                                            </div>
                                            <div class="group">
                                                <label class="block text-[10px] font-bold uppercase text-[#6e7976] mb-1">Primary Email</label>
                                                <input class="w-full bg-[#f1f4f2] border-none rounded-xl px-4 py-3 focus:ring-2 focus:ring-[#00695c] transition-all" 
                                                       placeholder="purchase@lumberinc.com" type="email" t-model="state.customerForm.email"/>
                                            </div>
                                            <div class="group">
                                                <label class="block text-[10px] font-bold uppercase text-[#6e7976] mb-1">Website</label>
                                                <input class="w-full bg-[#f1f4f2] border-none rounded-xl px-4 py-3 focus:ring-2 focus:ring-[#00695c] transition-all" 
                                                       placeholder="www.lumberinc.com" type="text" t-model="state.customerForm.website"/>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <footer class="mt-16 pt-8 border-t border-[#ebefec] flex flex-col md:flex-row justify-between items-center gap-6">
                                <div class="flex items-center text-[#6e7976] space-x-2">
                                    <span class="material-symbols-outlined text-sm">inventory_2</span>
                                    <span class="text-[10px] font-medium uppercase tracking-wider">Creating Customer Locally</span>
                                </div>
                                <div class="flex items-center space-x-4">
                                    <button class="px-8 py-3 text-sm font-bold text-[#6e7976] hover:text-[#181c1b] transition-colors"
                                            t-on-click="() => state.screen = 'products'">Discard</button>
                                    <button class="px-10 py-3 bg-[#004f45] text-white rounded-full font-manrope font-bold text-sm shadow-xl shadow-[#004f45] hover:scale-105 active:scale-95 transition-all"
                                            t-on-click="saveCustomer">Save Customer</button>
                                </div>
                            </footer>
                        </div>
                    </main>
                </div>

                <!-- Employee Selection Modal -->
                <div t-if="state.showEmployeeModal" class="fixed inset-0 z-[100] bg-[#181c1b]/40 backdrop-blur-sm flex items-center justify-center p-4 no-print">
                    <div class="bg-white rounded-2xl shadow-2xl overflow-hidden max-w-lg w-full flex flex-col border border-[#e0e3e1] animate-in fade-in zoom-in duration-200">
                        <div class="p-6 border-b border-[#e0e3e1] flex justify-between items-center bg-[#f7faf8]">
                            <h2 class="text-xl font-bold font-headline text-[#004f45]">Switch User</h2>
                            <button class="material-symbols-outlined text-[#5c635f] hover:text-[#181c1b] rounded-full p-2 hover:bg-[#e0e3e1] transition-colors" t-on-click="closeEmployeeModal">close</button>
                        </div>
                        <div class="p-4 grid grid-cols-1 gap-2 max-h-[60vh] overflow-y-auto">
                            <t t-foreach="state.employees" t-as="emp" t-key="emp.id">
                                <button class="flex items-center gap-4 p-4 rounded-xl hover:bg-[#ebefec] transition-colors border border-transparent hover:border-[#e0e3e1] text-left active:scale-[0.98]" t-on-click="() => this.selectEmployee(emp)">
                                    <div class="w-12 h-12 rounded-full bg-[#004f45] flex items-center justify-center text-white font-bold text-lg uppercase shadow-sm shrink-0">
                                        <t t-esc="emp.name[0]"/>
                                    </div>
                                    <div class="flex-1 min-w-0">
                                        <p class="font-bold font-headline text-[#181c1b] text-lg truncate" t-esc="emp.name"/>
                                        <p class="text-xs text-[#5c635f] truncate" t-esc="emp.job_id ? emp.job_id[1] : 'Employee'"/>
                                    </div>
                                    <span t-if="emp.pin" class="material-symbols-outlined text-[#5c635f] opacity-50 text-sm">lock</span>
                                </button>
                            </t>
                        </div>
                    </div>
                </div>

                <!-- PIN Entry Modal -->
                <div t-if="state.showPinModal" class="fixed inset-0 z-[110] bg-[#d7dbd8]/90 flex items-center justify-center p-4 no-print">
                    <main class="w-full max-w-sm bg-white rounded-xl shadow-[0_4px_16px_rgba(24,28,27,0.06)] overflow-hidden transition-all duration-300 relative animate-in fade-in slide-in-from-bottom-4">
                        <button class="absolute right-4 top-4 material-symbols-outlined text-[#5c635f] hover:text-[#181c1b]" t-on-click="closePinModal">close</button>
                        <!-- Header / Display Area -->
                        <div class="px-8 pt-10 pb-6 flex flex-col items-center gap-6">
                            <div class="flex flex-col items-center gap-2">
                                <span class="material-symbols-outlined text-[#004f45] text-4xl mb-1" style="font-variation-settings: 'FILL' 1;">lock</span>
                                <h1 class="font-headline text-[#181c1b] font-bold text-xl tracking-tight text-center" t-esc="state.selectedEmployee.name"/>
                                <p class="font-body text-[#5c635f] text-sm text-center">Enter Authorization PIN</p>
                            </div>

                            <div t-if="state.pinError" class="text-[#ba1a1a] bg-[#ffdad6] px-4 py-2 rounded-lg text-sm font-bold w-full text-center my-[-12px] z-10 animate-in fade-in slide-in-from-top-1">
                                <t t-esc="state.pinError"/>
                            </div>

                            <!-- PIN Display Area (Masked Dots) -->
                            <div class="flex items-center justify-center gap-4 py-4 h-12">
                                <t t-foreach="[1,2,3,4]" t-as="i" t-key="i">
                                    <div class="w-4 h-4 rounded-full transition-colors" t-att-class="state.pinBuffer.length >= i ? 'bg-[#004f45]' : 'bg-[#e0e3e1]'"></div>
                                </t>
                            </div>
                        </div>
                        <!-- Numeric Keypad Grid -->
                        <div class="px-8 pb-8">
                            <div class="grid grid-cols-3 gap-3">
                                <t t-foreach="['1','2','3','4','5','6','7','8','9']" t-as="btn" t-key="btn">
                                    <button class="aspect-square flex items-center justify-center bg-[#e0e3e1] text-[#181c1b] font-headline font-bold text-2xl rounded-xl hover:bg-[#d7dbd8] active:scale-95 transition-all" t-on-click="() => this.handlePinKeypad(btn)"><t t-esc="btn"/></button>
                                </t>
                                <button class="aspect-square flex flex-col items-center justify-center bg-transparent text-[#ba1a1a] font-body font-semibold text-xs uppercase tracking-widest rounded-xl hover:bg-[#ffdad6] active:scale-95 transition-all" t-on-click="() => this.handlePinKeypad('Clear')">
                                    Clear
                                </button>
                                <button class="aspect-square flex items-center justify-center bg-[#e0e3e1] text-[#181c1b] font-headline font-bold text-2xl rounded-xl hover:bg-[#d7dbd8] active:scale-95 transition-all" t-on-click="() => this.handlePinKeypad('0')">0</button>
                                <button class="aspect-square flex items-center justify-center bg-transparent text-[#5c635f] rounded-xl hover:bg-[#e0e3e1] active:scale-95 transition-all" t-on-click="() => this.handlePinKeypad('Backspace')">
                                    <span class="material-symbols-outlined text-2xl">backspace</span>
                                </button>
                            </div>
                            <!-- Prominent Action Button -->
                            <div class="mt-8">
                                <button class="w-full bg-[#004f45] text-white font-headline font-bold py-4 rounded-full shadow-[0_4px_12px_rgba(0,79,69,0.2)] hover:bg-[#00695c] active:scale-95 transition-all duration-150" t-on-click="authorizePin">
                                    Authorize
                                </button>
                            </div>
                        </div>
                        <!-- Subtle Gradient Decor -->
                        <div class="h-1 w-full bg-gradient-to-r from-[#004f45] to-[#00695c]"></div>
                    </main>
                </div>

                <!-- Barcode Entry Modal (Numeric) -->
                <div t-if="state.showBarcodeInput" class="fixed inset-0 z-[120] bg-[#181c1b]/60 backdrop-blur-md flex items-center justify-center p-4 no-print">
                    <main class="w-full max-w-sm bg-white rounded-3xl shadow-2xl overflow-hidden animate-in fade-in zoom-in duration-300 border border-[#e0e3e1]">
                        <!-- Dynamic Header -->
                        <div class="bg-[#f7faf8] p-8 flex flex-col items-center gap-4 border-b border-[#e0e3e1]">
                            <div class="w-16 h-16 bg-[#004f45] rounded-2xl flex items-center justify-center text-white shadow-lg shadow-[#004f45]/20">
                                <span class="material-symbols-outlined text-3xl" style="font-variation-settings: 'FILL' 1;">scan</span>
                            </div>
                            <div class="text-center">
                                <h2 class="font-headline text-2xl font-black text-[#004f45] tracking-tight">Enter Barcode</h2>
                                <p class="text-xs font-bold text-[#5c635f] uppercase tracking-widest mt-1 opacity-60">Manual SKU/Barcode Input</p>
                            </div>
                        </div>

                        <!-- Buffer Display -->
                        <div class="px-8 py-6">
                            <div class="relative group">
                                <input class="w-full text-center text-4xl font-headline font-black bg-[#f1f4f2] text-[#004f45] py-5 rounded-2xl border-none focus:ring-2 focus:ring-[#004f45] transition-all tracking-tighter" 
                                       placeholder="••••••••" type="text"
                                       t-model="state.barcodeInput"
                                       t-on-keydown="onBarcodeKeydown"
                                       t-ref="barcodeInputRef"
                                       autofocus="autofocus"/>
                                <div t-if="!state.barcodeInput" class="absolute inset-0 flex items-center justify-center pointer-events-none opacity-20 italic text-sm">Waiting for input...</div>
                            </div>
                        </div>

                        <!-- Numerical Keypad -->
                        <div class="px-8 pb-4">
                            <div class="grid grid-cols-3 gap-3">
                                <t t-foreach="['1','2','3','4','5','6','7','8','9']" t-as="n" t-key="n">
                                    <button class="h-14 flex items-center justify-center bg-[#f7faf8] text-[#181c1b] font-headline font-black text-2xl rounded-2xl hover:bg-[#ebefec] active:scale-95 transition-all border border-[#e0e3e1]/50" 
                                            t-on-click="() => state.barcodeInput += n"><t t-esc="n"/></button>
                                </t>
                                <button class="h-14 flex items-center justify-center bg-transparent text-[#ba1a1a] font-bold text-xs uppercase tracking-tighter rounded-2xl hover:bg-red-50 transition-all" 
                                        t-on-click="() => state.barcodeInput = ''">Clear</button>
                                <button class="h-14 flex items-center justify-center bg-[#f7faf8] text-[#181c1b] font-headline font-black text-2xl rounded-2xl hover:bg-[#ebefec] active:scale-95 transition-all border border-[#e0e3e1]/50" 
                                        t-on-click="() => state.barcodeInput += '0'">0</button>
                                <button class="h-14 flex items-center justify-center bg-transparent text-[#5c635f] rounded-2xl hover:bg-[#ebefec] active:scale-95 transition-all" 
                                        t-on-click="() => state.barcodeInput = state.barcodeInput.slice(0, -1)">
                                    <span class="material-symbols-outlined text-2xl">backspace</span>
                                </button>
                            </div>
                        </div>

                        <!-- Primary Actions -->
                        <footer class="p-8 flex gap-3">
                            <button class="flex-1 py-4 bg-white text-[#5c635f] border-2 border-[#e0e3e1] rounded-2xl font-headline font-bold text-sm uppercase tracking-wider hover:bg-[#f7faf8] transition-all" 
                                    t-on-click="toggleBarcodeInput">Cancel</button>
                            <button class="flex-[2] py-4 bg-[#004f45] text-white rounded-2xl font-headline font-black text-sm uppercase tracking-[0.1em] shadow-xl shadow-[#004f45]/20 hover:opacity-90 active:scale-95 transition-all disabled:opacity-30" 
                                    t-att-disabled="!state.barcodeInput"
                                    t-on-click="() => { this.addProductByBarcode(state.barcodeInput); state.barcodeInput = ''; state.showBarcodeInput = false; }">
                                Add Product
                            </button>
                        </footer>
                        <div class="h-1.5 w-full bg-gradient-to-r from-[#004f45] to-[#00695c]"></div>
                    </main>
                </div>

                <!-- Backend Note Modal (purple theme = backend identifier) -->
                <div t-if="state.showNoteModal" class="fixed inset-0 z-[150] bg-[#181c1b]/60 backdrop-blur-md flex items-center justify-center p-4 animate-in fade-in duration-200 no-print">
                    <main class="w-full max-w-lg bg-white rounded-3xl shadow-2xl overflow-hidden border border-[#7c3aed]/20 animate-in zoom-in slide-in-from-bottom-4 duration-200">
                        <div class="bg-gradient-to-r from-[#7c3aed] to-[#5b21b6] px-8 py-6 flex items-center gap-4">
                            <div class="w-12 h-12 rounded-2xl bg-white/15 backdrop-blur flex items-center justify-center text-white">
                                <span class="material-symbols-outlined text-2xl" style="font-variation-settings: 'FILL' 1;">sticky_note_2</span>
                            </div>
                            <div class="flex-1">
                                <h2 class="font-headline font-black text-white text-xl tracking-tight">Backend Note</h2>
                                <p class="text-white/80 text-xs uppercase tracking-widest font-bold">For internal identification only</p>
                            </div>
                            <button class="material-symbols-outlined text-white/80 hover:text-white rounded-full p-2 hover:bg-white/10 transition-colors" t-on-click="closeNoteModal">close</button>
                        </div>
                        <div class="px-8 py-6 space-y-4">
                            <label class="block text-[10px] font-bold uppercase tracking-widest text-[#7c3aed]">Internal Note</label>
                            <textarea class="w-full bg-[#f5f3ff] border border-[#7c3aed]/20 rounded-2xl p-4 text-sm text-[#3b0764] focus:ring-2 focus:ring-[#7c3aed] focus:outline-none transition-all resize-none"
                                      rows="6"
                                      placeholder="Add a note for the backend (will appear in the order's chatter)..."
                                      t-model="state.noteDraft"/>
                            <div class="flex items-start gap-2 text-[11px] text-[#5c635f] bg-[#f7faf8] p-3 rounded-xl border border-[#e0e3e1]">
                                <span class="material-symbols-outlined text-[#7c3aed] text-base">info</span>
                                <span>This note will be posted to the sale order's chatter when the order is synced to the backend. It is also stored locally while offline.</span>
                            </div>
                        </div>
                        <footer class="px-8 pb-8 flex gap-3">
                            <button class="flex-1 py-3 bg-white text-[#5c635f] border-2 border-[#e0e3e1] rounded-2xl font-headline font-bold text-sm uppercase tracking-wider hover:bg-[#f7faf8] transition-all"
                                    t-on-click="closeNoteModal">Cancel</button>
                            <button class="flex-[2] py-3 bg-[#7c3aed] text-white rounded-2xl font-headline font-black text-sm uppercase tracking-[0.1em] shadow-xl shadow-[#7c3aed]/30 hover:bg-[#6d28d9] active:scale-95 transition-all"
                                    t-on-click="saveBackendNote">Save Note</button>
                        </footer>
                        <div class="h-1.5 w-full bg-gradient-to-r from-[#7c3aed] to-[#5b21b6]"></div>
                    </main>
                </div>

                <!-- Email Composer Modal -->
                <div t-if="state.emailModal and state.emailModal.show" class="fixed inset-0 z-[160] bg-[#181c1b]/60 backdrop-blur-md flex items-center justify-center p-4 animate-in fade-in duration-200 no-print">
                    <main class="w-full max-w-lg bg-white rounded-3xl shadow-2xl overflow-hidden animate-in zoom-in slide-in-from-bottom-4 duration-200 border border-[#004f45]/20">
                        <div class="bg-gradient-to-r from-[#004f45] to-[#00695c] px-8 py-6 flex items-center gap-4">
                            <div class="w-12 h-12 rounded-2xl bg-white/15 backdrop-blur flex items-center justify-center text-white">
                                <span class="material-symbols-outlined text-2xl">mail</span>
                            </div>
                            <div class="flex-1">
                                <h2 class="font-headline font-black text-white text-xl tracking-tight">Compose Email</h2>
                                <p class="text-white/80 text-xs uppercase tracking-widest font-bold">Send Receipt as PDF</p>
                            </div>
                            <button class="material-symbols-outlined text-white/80 hover:text-white rounded-full p-2 hover:bg-white/10 transition-colors" t-on-click="closeEmailModal">close</button>
                        </div>
                        <div class="px-8 py-6 space-y-4">
                            <div class="space-y-2">
                                <label class="block text-[10px] font-bold uppercase tracking-widest text-[#004f45]">To</label>
                                <input class="w-full bg-[#f1f4f2] border-none rounded-xl px-4 py-3 focus:ring-2 focus:ring-[#00695c] transition-all" 
                                       placeholder="customer@example.com" type="email" t-model="state.emailModal.to"/>
                            </div>
                            <div class="space-y-2">
                                <label class="block text-[10px] font-bold uppercase tracking-widest text-[#004f45]">Subject</label>
                                <input class="w-full bg-[#f1f4f2] border-none rounded-xl px-4 py-3 focus:ring-2 focus:ring-[#00695c] transition-all font-bold" 
                                       type="text" t-model="state.emailModal.subject"/>
                            </div>
                            <div class="space-y-2">
                                <label class="block text-[10px] font-bold uppercase tracking-widest text-[#004f45]">Body</label>
                                <textarea class="w-full bg-[#f1f4f2] border-none rounded-xl p-4 text-sm focus:ring-2 focus:ring-[#00695c] focus:outline-none transition-all resize-none"
                                          rows="6"
                                          t-model="state.emailModal.body"/>
                            </div>
                            <div class="flex items-start gap-2 text-[11px] text-[#5c635f] bg-[#f7faf8] p-3 rounded-xl border border-[#e0e3e1]">
                                <span class="material-symbols-outlined text-[#004f45] text-base">picture_as_pdf</span>
                                <span>The transaction receipt PDF will be automatically attached to this email.</span>
                            </div>
                        </div>
                        <footer class="px-8 pb-8 flex gap-3">
                            <button class="flex-1 py-3 bg-white text-[#5c635f] border-2 border-[#e0e3e1] rounded-2xl font-headline font-bold text-sm uppercase tracking-wider hover:bg-[#f7faf8] transition-all"
                                    t-on-click="closeEmailModal">Cancel</button>
                            <button class="flex-[2] py-3 bg-[#004f45] text-white rounded-2xl font-headline font-black text-sm uppercase tracking-[0.1em] shadow-xl shadow-[#004f45]/30 hover:bg-[#00695c] active:scale-95 transition-all flex items-center justify-center gap-2"
                                    t-att-disabled="!state.emailModal.to"
                                    t-on-click="onEmailModalSend">
                                <span class="material-symbols-outlined">send</span> Send Email
                            </button>
                        </footer>
                    </main>
                </div>

                <!-- Error/Notification Popup Modal -->
                <div t-if="state.errorPopup.show" class="fixed inset-0 z-[200] bg-[#181c1b]/60 backdrop-blur-md flex items-center justify-center p-4 animate-in fade-in duration-300 no-print">
                    <div class="bg-white rounded-3xl shadow-2xl overflow-hidden max-w-sm w-full flex flex-col border border-[#e0e3e1] animate-in zoom-in slide-in-from-bottom-8 duration-300">
                        <div class="px-8 pt-10 pb-6 flex flex-col items-center text-center gap-6">
                            <div class="w-20 h-20 rounded-full flex items-center justify-center shadow-lg"
                                 t-att-class="state.errorPopup.type === 'error' ? 'bg-[#ffdad6] text-[#ba1a1a]' : 'bg-[#a0f2e1] text-[#00382d]'">
                                <span class="material-symbols-outlined text-4xl" style="font-variation-settings: 'opsz' 48, 'wght' 600;">
                                    <t t-if="state.errorPopup.type === 'error'">error</t>
                                    <t t-else="">check_circle</t>
                                </span>
                            </div>
                            <div class="space-y-2">
                                <h2 class="text-2xl font-black font-headline text-[#004f45] uppercase tracking-tighter" t-esc="state.errorPopup.title"/>
                                <p class="text-[#5c635f] font-medium leading-relaxed" t-esc="state.errorPopup.message"/>
                            </div>
                        </div>
                        <div class="p-8 bg-[#f7faf8] border-t border-[#e0e3e1]">
                            <button class="w-full py-4 rounded-2xl font-headline font-black uppercase tracking-widest text-sm shadow-xl transition-all active:scale-[0.98] hover:opacity-90"
                                    t-on-click="closePopup"
                                    t-att-class="state.errorPopup.type === 'error' ? 'bg-[#ba1a1a] text-white shadow-[#ba1a1a]/20' : 'bg-[#004f45] text-white shadow-[#004f45]/20'">
                                OK
                            </button>
                        </div>
                        <div t-att-class="state.errorPopup.type === 'error' ? 'bg-[#ba1a1a]' : 'bg-[#004f45]'" class="h-1.5 w-full"></div>
                    </div>
                </div>

            </div>
        </div>
    </t>
</templates>
