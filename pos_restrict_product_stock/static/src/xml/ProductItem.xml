<?xml version="1.0" encoding="UTF-8"?>
<!--inherited to add quantity in  each product card-->
<templates id="template" xml:space="preserve">
    <t t-name="point_of_sale.ProductCard" t-inherit="point_of_sale.ProductCard"
       t-inherit-mode="extension">
        <xpath expr="//div[hasclass('product-information-tag')]"
               position="after">
            <t t-if="this.env.services.pos.config.is_display_stock">
                <t t-if="this.env.services.pos.config.stock_type == 'qty_on_hand'">
                    <div id="qty_display"
                      t-attf-class="display_qty #{props.product.qty_available lte 0 ? 'not-available' : ''}">
                     <t t-esc="props.product.qty_available"/>
                 </div>
                </t>
                <t t-if="this.env.services.pos.config.stock_type == 'virtual_qty'">
                    <div t-attf-class="display_virtual #{props.product.virtual_available lte 0 ? 'not-available':''}">
                        <t t-esc="props.product.virtual_available"/>
                    </div>
                </t>
                <t t-if="this.env.services.pos.config.stock_type == 'both'">
                    <div id="qty_display"
                         t-attf-class="display_qty #{props.product.qty_available lte 0 ? 'not-available' : ''}">
                        <t t-esc="props.product.qty_available"/>
                    </div>
                    <div t-attf-class="display_virtual #{props.product.virtual_available 0 ? 'not-available':''}">
                        <t t-esc="props.product.virtual_available"/>
                    </div>
                </t>
            </t>
        </xpath>
    </t>
</templates>
