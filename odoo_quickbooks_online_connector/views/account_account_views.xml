<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Inherited account_account model form view to add fields related to quickbook -->
    <record id="view_account_form" model="ir.ui.view">
        <field name="name">account.account.view.form.inherit.odoo.quickbooks.online.connector</field>
        <field name="model">account.account</field>
        <field name="inherit_id" ref="account.view_account_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='accounting']" position="inside">
                <group>
                    <group name="qbook" string="QuickBooks Data">
                        <field name="qbooks_account" readonly="1"/>
                        <field name="qbooks_sync_token" readonly="1"/>
                    </group>
                </group>
            </xpath>
        </field>
    </record>
    <!-- Actions -->
    <record id="action_quickbook_accounts" model="ir.actions.act_window">
        <field name="name">Chart of Accounts</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">account.account</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('qbooks_account', '!=', False)]</field>
        <field name="context">{'create': 0}</field>
    </record>
</odoo>
