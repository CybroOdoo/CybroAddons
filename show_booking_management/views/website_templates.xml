<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--  Movie show template  -->
    <template id="show_movie">
        <t t-call="website.layout">
            <div id="snippetContent">
                <div class="container" id="main_layout">
                    <section class="content">
                        <div class="row">
                            <div class="d-contents">
                                <t t-foreach="movies" t-as="movie"
                                   t-key="movie.id">
                                    <div class="card" style="position: relative;">
                                        <div class="card-body d-flex">
                                            <t t-if="movie.state == 'prebooking'">
                                                <div class="ribbon-wrapper">
                                                    <div class="ribbon">Pre-booking</div>
                                                </div>
                                            </t>
                                            <div>
                                                <div style="display:grid;justify-content:center;">
                                                    <img t-attf-src="data:image/png;base64,#{movie.movie_poster}"
                                                         alt="Movie Poster"
                                                         width="185px"
                                                         height="275px;"/>
                                                </div>
                                                <div style="margin-top:10px;display:grid;justify-content:center;">
                                                    <h5 class="movie-name">
                                                        <b>
                                                            <t t-esc="movie.name"/>
                                                        </b>
                                                    </h5>
                                                </div>
                                                <div>
                                                    <t t-if="movie.is_prebooking_closed">
                                                        <a href="javascript:void(0)"
                                                           class="btn btn-secondary d-block disabled"
                                                           style="background-color: grey; border-color: grey; pointer-events: none;">
                                                            Closed
                                                        </a>
                                                    </t>
                                                    <t t-else="">
                                                        <a t-att-href="'/book_now/' + str(movie.id)"
                                                           class="btn btn-primary d-block">
                                                            Book Now
                                                        </a>
                                                    </t>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </section>
                </div>
            </div>
        </t>
    </template>

    <!--  Book Show template  -->
   <template id="book_movie" t-name="BookShow">
    <t t-call="website.layout">
        <div id="snippetContent">
            <div class="container" id="main_layout">
                <div class="card mt-3 mb-3">
                    <!-- Main Content Row -->
                    <div class="row g-3 p-3" style="background-color: slategray;">

                        <!-- Movie Poster -->
                        <div class="col-12 col-md-4 text-center">
                            <img t-attf-src="data:image/png;base64,#{movie.movie_poster}"
                                 class="img-fluid rounded"
                                 alt="Movie Poster"/>
                        </div>

                        <!-- Movie Details -->
                        <div class="col-12 col-md-4">
                            <h3><t t-esc="movie.name"/></h3>
                            <p>
                                <span t-if="movie.movie_language_id.name">
                                    <i class="fa fa-globe"></i> <t t-esc="movie.movie_language_id.name"/>
                                </span>
                            </p>
                            <p><i class="fa fa-calendar-o"></i> <t t-esc="movie.release_date"/></p>
                            <p><i class="fa fa-clock-o"></i> <t t-esc="movie.duration"/> Hrs</p>
                            <p>
                                <i class="fa fa-calendar"></i> <t t-esc="movie.show_start_date"/>
                                <i class="fa fa-long-arrow-right"></i> <t t-esc="movie.show_end_date"/>
                            </p>
                            <div class="d-flex flex-wrap gap-1">
                                <t t-foreach="movie.show_type_ids" t-as="show_type">
                                    <span class="badge bg-primary"><t t-esc="show_type.name"/></span>
                                </t>
                            </div>
                        </div>

                        <!-- Booking Form -->
                        <div class="col-12 col-md-4">
                            <div class="book_show">
                                <form class="form-control" action="/movie/book_ticket" method="post"
                                      t-att-data-movie-id="movie.id">
                                    <input type="hidden" name="movie_id" t-att-value="movie.id"/>

                                    <h4 class="text-center mt-2">Book Tickets</h4>

                                    <div class="mb-2">
                                        <label>Date:</label>
                                        <input id="choose_date" name="show_date" type="date" class="form-control" required="1"/>
                                        <span id="error_box" class="text-danger" style="display:none;"></span>
                                    </div>

                                    <div class="mb-2">
                                        <label>Screen:</label>
                                        <select name="screen" class="form-control" required="1">
                                            <t t-foreach="movie.available_screens_ids" t-as="screen">
                                                <option t-att-value="screen.id"><t t-esc="screen.name"/></option>
                                            </t>
                                        </select>
                                    </div>

                                    <div class="mb-2">
                                        <label>Time Slots:</label>
                                        <select name="time_slots" class="form-control" required="1">
                                            <t t-foreach="movie.available_time_slots_ids" t-as="time_slots">
                                                <option t-att-value="time_slots.id"><t t-esc="time_slots.name"/></option>
                                            </t>
                                        </select>
                                    </div>

                                    <div class="text-center mt-3">
                                        <button type="submit" class="btn btn-primary w-100">
                                            Select Seats
                                        </button>
                                    </div>
                                </form>
                            </div>
                        </div>

                    </div>

                    <!-- Movie Overview -->
                    <div class="mt-4 mx-3">
                        <h4 t-if="movie.about_movie">Overview</h4>
                        <hr/>
                        <div><t t-esc="movie.about_movie"/></div>
                    </div>

                    <!-- Movie Cast -->
                    <div class="mt-4 mx-3">
                        <h4 t-if="movie.movie_cast_ids">Show Cast</h4>
                        <hr/>
                        <div class="row g-3 pb-3">
                            <t t-foreach="movie.movie_cast_ids" t-as="cast">
                                <div class="col-6 col-md-3 col-lg-2 text-center">
                                    <t t-if="cast.cast_image">
                                        <img t-attf-src="data:image/png;base64,#{cast.cast_image}"
                                             class="img-fluid rounded-circle"
                                             style="max-width: 100px;"/>
                                    </t>
                                    <t t-else="">
                                        <img src="/show_booking_management/static/src/img/avatar-default.svg"
                                             class="img-fluid rounded-circle"
                                             style="max-width: 100px;"/>
                                    </t>
                                    <div class="mt-1"><t t-esc="cast.name"/></div>
                                    <div class="text-muted badge bg-info mt-1"><t t-esc="cast.cast_id.name"/></div>
                                </div>
                            </t>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </t>
</template>


    <!--  Seat selection template  -->
    <template id="seat_selection_template" name="Seat Selection">
        <t t-call="website.layout">
            <div class="seat-selection-wrapper">
                <div class="container mt-5 seat-selection">
                    <form id="seat-selection-form"
                          action="/movie/confirm_booking" method="post">
                        <div style="display:flex;justify-content:center;">
                            <div>
                                <div class="h2"
                                     style="text-align: center;font-family: monospace;margin-bottom:30px;">
                                    Select Your Seats
                                </div>
                                <div style="padding:15px;display:flex;justify-content:space-evenly;">
                                    <t t-foreach="movie.available_time_slots_ids"
                                       t-as="time_slot">
                                        <div t-att-class="'time-slot-btn btn btn-outline-info active' if time_slot_id.id == time_slot.id else 'time-slot-btn btn btn-outline-info'"
                                             t-att-data-time-slot-id="time_slot.id">
                                            <t t-esc="time_slot.name"/>
                                        </div>
                                    </t>
                                </div>
                            </div>
                            <div class="button-container" style="display:none">
                                <button type="submit" class="btn btn-primary confirm_booking"
                                        style="margin:10px;">Confirm Booking
                                </button>
                            </div>
                        </div>
                        <input type="hidden" name="csrf_token"
                               t-att-value="request.csrf_token()"/>
                        <div class="seating-chart-container">
                            <div class="seating-chart">
                                <t t-foreach="range(screen.total_rows)"
                                   t-as="row">
                                    <div class="seat-row">
                                        <div class="seat-row-inner">
                                            <div style="width: max-content;">
                                                Row
                                                <t t-esc="row + 1"/>
                                            </div>
                                            <t t-foreach="range(screen.no_of_seat_row)"
                                               t-as="col">
                                                <t t-set="seat_id"
                                                   t-value="'R' + str(row + 1) + 'C' + str(col + 1)"/>
                                                <div class="seat-wrapper">
                                                    <input type="checkbox"
                                                           class="seat-checkbox"
                                                           t-att-id="'seat-' + str(row + 1) + '-' + str(col + 1)"
                                                           t-att-name="'selected_seats'"
                                                           t-att-value="'R' + str(row + 1) + 'C' + str(col + 1)"
                                                           t-att-disabled="seat_id in booked_seats"
                                                           t-att-checked="seat_id in booked_seats"/>
                                                    <label t-att-for="'seat-' + str(row +1) + '-' + str(col + 1)"
                                                           class="seat-label">
                                                        <t t-esc="col + 1"/>
                                                    </label>
                                                </div>
                                            </t>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </div>
                        <div class="selected-seats" style="text-align:center;display:none">
                            <label class="seat-demo-selected">0</label>
                            Selected
                        </div>
                        <input type="hidden" name="screen_id"
                               t-att-value="screen.id"/>
                        <input type="hidden" name="time_slot_id"
                               t-att-value="time_slot_id.id"/>
                        <input type="hidden" name="registration_id"
                               t-att-value="registration_id"/>
                        <input type="hidden" name="movie_id"
                               t-att-value="movie.id"/>
                        <input type="hidden" name="booking_date"
                               t-att-value="booking_date"/>
                        <div class="screen">
                            <h3>Screen</h3>
                        </div>
                    </form>
                </div>
                <div class="container mt-3 sidebar">
                    <div class="h3" style="text-align:center;">
                        Details
                    </div>
                    <hr/>
                    <table class="table">
                        <tr>
                            <th>Movie</th>
                            <td>
                                <t t-esc="movie.name"/>
                            </td>
                        </tr>
                        <tr>
                            <th>Screen</th>
                            <td>
                                <t t-esc="screen.name"/>
                            </td>
                        </tr>
                        <tr>
                            <th>Time Slot</th>
                            <td class="sidebar_timeslot">
                                <t t-esc="time_slot_id.name"/>
                            </td>
                        </tr>
                        <tr>
                            <th>Booking Date</th>
                            <td>
                                <t t-esc="booking_date"/>
                            </td>
                        </tr>
                        <tr>
                            <td>
                                <label class="seat-demo-available">
                                    <t t-esc="available_seats_count"/>
                                </label>
                            </td>
                            <td>
                                Available
                            </td>
                        </tr>
                        <tr>
                            <td>
                                <label class="seat-demo-booked">
                                    <t t-esc="booked_seats_count"/>
                                </label>
                            </td>
                            <td>
                                Booked
                            </td>
                        </tr>
                    </table>
                </div>
            </div>
        </t>
    </template>
</odoo>
