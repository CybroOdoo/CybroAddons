<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_task_form2" model="ir.ui.view">
        <!--Inherit Project from view-->
        <field name="name">project.task.view.form.inherit.allocation.time.approval</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="timesheet_grid.project_task_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_timer_start' and contains(@class, 'btn-primary')]" position="attributes">
            <attribute name="attrs">
                {'invisible': ['|','|','|',
                    ('display_timer_start_primary', '=', False),
                    ('encode_uom_in_days', '=', True),
                    ('new_stage', '=', True),
                    ('to_approve_stage', '=', True)
                ]}
            </attribute>
        </xpath>
            <xpath expr="//header" position="inside">
                    <button type="object" name="action_approval" string="Manager Approval"
                            attrs="{'invisible':[('new_stage','=',False)]}" class="btn btn-primary"/>
                    <button type="object" name="action_done" string=" Done"
                            attrs="{'invisible':[
                            ('to_progress_stage','=',False)]}"/>
                    <button type="object" name="action_cancel" string="Cancel"
                            attrs="{'invisible':[
                            ('to_progress_stage','=',False)]}">
                    </button>
            </xpath>
            <!--Add certain fields to Project form view-->
            <field name="tag_ids" position="after">
                <field name="new_stage" invisible="1"/>
                <field name="to_approve_stage" invisible="1"/>
                <field name="to_progress_stage" invisible="1"/>
                <field name="task_create_boolean"/>
            </field>
            <!--Make stage as readonly-->
            <xpath expr="//field[@name='stage_id']" position="attributes">
                <attribute name="options"
                >{'readonly':[('to_approve_stage', '=', True)]}</attribute>
            </xpath>
            <!--Make Planned Hours as readonly-->
            <xpath expr="//field[@name='planned_hours']" position="attributes">
                <attribute name="attrs"
                >{'readonly':[('to_progress_stage', '=', True)]}</attribute>
            </xpath>
        </field>
    </record>
</odoo>
