<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <!-- Action to generate the Prescription Report in PDF format -->
    <record id="action_report_prescription" model="ir.actions.report">
        <field name="name">Prescription Report</field>
        <field name="model">prescription.orders</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">veterinary_management.report_prescription</field>
        <field name="report_file">veterinary_management.report_prescription</field>
        <field name="binding_model_id" ref="model_prescription_orders"/>
        <field name="binding_type">report</field>
    </record>
</odoo>
