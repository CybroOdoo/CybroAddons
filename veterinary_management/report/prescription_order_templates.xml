<odoo>
    <!--  Pdf template of Prescription Order Report -->
    <template id="report_prescription">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="o">
                <div class="page">
                    <style>
                        .page {
                            padding: 20px;
                            font-family: Arial, sans-serif;
                        }

                        h2 {
                            text-align: center;
                            font-size: 24px;
                            margin-bottom: 20px;
                        }

                        /* Table styling */
                        .table {
                            width: 100%;
                            border-collapse: collapse;
                            margin-bottom: 20px;
                        }

                        .table thead {
                            background-color: #f2f2f2;
                        }

                        .table th, .table td {
                            border: 1px solid #ddd;
                            padding: 8px;
                            text-align: left;
                        }

                        .table th {
                            background-color: #4CAF50;
                            color: white;
                        }

                        .table tbody tr:nth-child(even) {
                            background-color: #f9f9f9;
                        }

                        .table tbody tr:hover {
                            background-color: #f1f1f1;
                        }

                        .text-bold {
                            font-weight: bold;
                        }
                    </style>
                    <h2>Prescription</h2>
                    <table class="table table-condensed">
                        <thead>
                            <tr>
                                <th>Medicine</th>
                                <th>Form</th>
                                <th>Duration(Days)</th>
                                <th>Frequency</th>
                                <th>Intake Time</th>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-foreach="o.prescription_line_ids" t-as="line">
                                <tr>
                                    <td><span t-field="line.medicine_id.name"/></td>
                                    <td><span t-field="line.form"/></td>
                                    <td><span t-field="line.duration"/></td>
                                    <td><span t-field="line.frequency"/></td>
                                    <td><span t-field="line.intake_time"/></td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                </div>
            </t>
        </t>
    </template>
</odoo>
