<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- View: wm.partner.contract.line - wm.partner.contract.line.list (List) -->
    <record id="action_contract_line_list_view" model="ir.ui.view">
        <field name="name">wm.partner.contract.line.list</field>
        <field name="model">wm.partner.contract.line</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="category_id" string="Product"/>
            </list>
        </field>
    </record>
</odoo>
