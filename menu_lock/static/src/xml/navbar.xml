<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-inherit="web.NavBar" t-inherit-mode="extension" owl="1">
        <xpath expr="//nav[hasclass('o_main_navbar')]" position="after">
            <t t-if="!lock_state.locked">
                <div class="o_menu_lock_overlay">
                    <div class="o_menu_lock_modal">
                        <h4>Menu Security PIN</h4>
                        <div class="position-relative password-wrapper">
                            <input type="password"
                                   id="password"
                                   t-ref="password"
                                   class="password-input"
                                   placeholder="Enter Password"/>

                            <i class="fa fa-eye toggle_eye"
                               t-on-click="showPassword"/>
                        </div>
                        <div class="menu_warning d-none" t-ref="menu_warn">
                           <div id="wrap">
                                <div class="container">
                                    <div class="col-md-12">
                                        <div class="alert alert-danger">
                                            Wrong Password !!!!
                                            Please enter correct password.
                                        </div>
                                    </div>
                                </div>
                            </div>
                       </div>
                        <div class="mt-4 text-end">
                            <button class="btn btn-primary me-2"
                                    t-on-click="Confirm">
                                Confirm
                            </button>

                            <button class="btn btn-secondary"
                                    t-on-click="CancelDialog">
                                Cancel
                            </button>
                        </div>
                    </div>
                </div>
            </t>
        </xpath>
    </t>
</templates>