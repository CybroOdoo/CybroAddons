<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- QR Code On Full page ticket -->
    <template id="event_report_template_full_page_ticket_inherited"
              inherit_id="event.event_report_template_full_page_ticket">
        <xpath expr="//div[hasclass('o_event_full_page_left_details_top')]"
               position="inside">
            <t t-if="attendee and attendee.event_ticket_id and attendee.event_ticket_id.ticket_qr_code_image">
                <h5>Scan The QR Code for Tickets</h5>
                <img style="height: 100px; width: 100px;"
                     t-att-src="'data:image/png;base64,' + (attendee.event_ticket_id.ticket_qr_code_image.decode('utf-8') if isinstance(attendee.event_ticket_id.ticket_qr_code_image, bytes) else attendee.event_ticket_id.ticket_qr_code_image)"/>
            </t>
        </xpath>
    </template>
</odoo>