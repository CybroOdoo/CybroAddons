<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <record id="seq_account_bulk_payment" model="ir.sequence">
            <field name="name">Bulk Payment</field>
            <field name="code">account.bulk.payment</field>
            <field name="prefix">BULK/</field>
            <field name="padding">4</field>
            <!-- Global (all companies) so numbering works in every company. -->
            <field name="company_id" eval="False"/>
        </record>
    </data>
</odoo>
