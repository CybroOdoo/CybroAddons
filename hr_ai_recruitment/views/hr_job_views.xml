<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_hr_job_form" model="ir.ui.view">
        <field name="name">view.hr.job.form.inherit.hr_ai_recruitment</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr.view_hr_job_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="before">
                <header>
                    <field name="ai_shortlist_enabled" invisible="1"/>
                    <button type='object' name="action_shortlist"
                            string="Shortlist" class="oe_highlight"
                            invisible="not ai_shortlist_enabled"/>
                </header>
            </xpath>
            <xpath expr="//page[@name='job_description_page']" position="after">
                <page string="Shortlisted Applicants"
                      name="shortlist_applicant" invisible="not shortlisted_applicant_ids">
                    <field name="shortlisted_applicant_ids">
                        <tree>
                            <field name="partner_name"/>
                            <field name="create_date"/>
                            <field name="name"/>
                            <field name="stage_id"/>
                            <field name="ats_score"/>
                            <field name="categ_ids" widget="many2many_tags"/>
                            <field name="user_id"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
