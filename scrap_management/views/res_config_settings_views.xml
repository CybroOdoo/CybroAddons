<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--Inherited form view of configuration settings of stock to add field
    move to scrap in settings-->
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">
            res.config.settings.view.form.inherit.scrap.management
        </field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="stock.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//setting[@id='group_lot_on_delivery_slip']"
                   position="after">
                <setting help="Move to Scrap for Expired Products"
                invisible="module_product_expiry == False">
                    <field name="move_to_scrap"/>
                           <div class="content-group"
                         invisible="move_to_scrap == False">
                        <div class="mt16">
                            <span class="col-lg-3">After:
                                <field name="move_to_scrap_days"
                                       required="move_to_scrap == True"/>
                                days
                            </span>
                        </div>
                    </div>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
