<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Res config settings view form-->
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">
            res.config.settings.view.form.inherit.sales_person_signature
        </field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="10"/>
        <field name="inherit_id" ref="sale.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@name='quotation_order_setting_container']"
                   position="after">
                <block title="Sale Approval"
                       name="Sale_approval_setting_container">
                    <setting id="enable_sale_approval"
                             title="The users assigned for stock approvals"
                             help="Enable this field to make the sale order validate button visible/invisible based on salesperson signature.">
                        <field name="is_sale_document_approve"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
