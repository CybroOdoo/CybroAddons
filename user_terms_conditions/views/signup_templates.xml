<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Inherited to add the terms and conditions -->
    <template id="auth_fields" inherit_id="auth_signup.fields"
              name="user Terms and conditions">
        <xpath expr="//input[@name='confirm_password']/.."
               position="after">
            <div class="form-group o_boolean_toggle mt-2">
                <input type="checkbox" class="form-check-input"
                       name="terms_condition" id="terms_condition"
                       required="required"/>
                <label for="terms_condition" class="form-check-label">
                    <a t-attf-href="/terms?{{ keep_query() }}"
                       class="btn btn-link btn-sm" role="button">
                        Terms and Conditions
                    </a>
                </label>
            </div>
        </xpath>
    </template>
</odoo>
