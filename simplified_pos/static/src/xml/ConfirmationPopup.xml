<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <!-- A template ConfirmationPopup is created for the confirmation popup -->
    <t t-name="ConfirmationPopup" owl="1">
        <Dialog size="'md'" title="props.title" modalRef="modalRef">
            <t t-set-slot="footer">
                <div style="display: flex; justify-content: center; align-items: center; gap: 10px; padding: 10px; width: 100%; box-sizing: border-box;">
                    <button class="button confirm" t-on-click="confirm" style="background-color: #4CAF50; color: white; padding: 8px 16px; border: none; border-radius: 4px; cursor: pointer;">
                        <t t-esc="props.confirmText || 'Confirm'"/>
                    </button>
                    <button class="button cancel" t-on-click="cancel" style="background-color: #ccc; color: #333; padding: 8px 16px; border: 1px solid #ccc; border-radius: 4px; cursor: pointer;">
                        <t t-esc="props.cancelText || 'Cancel'"/>
                    </button>
                </div>
            </t>
            <main class="body" style="display: flex; flex-direction: column; align-items: center; padding: 20px;">
                <div t-if="props.bodyText" style="text-align: center; color: #333;">
                    <t t-esc="props.bodyText"/>
                </div>
            </main>
        </Dialog>
    </t>
</templates>
