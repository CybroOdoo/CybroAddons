<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Extend user form to configure POS button visibility and session restrictions. -->
    <record id="view_users_form" model="ir.ui.view">
        <field name="name">res.users.view.form.inherit.pos.button.visibility</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='page_security']" position="after">
                <page string="Point of Sale" name="point_of_sales">
                    <group>
                        <field name="pos_session_ids" widget="many2many_tags"/>
                        <field name="pos_button_ids"
                               widget="many2many_checkboxes"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>