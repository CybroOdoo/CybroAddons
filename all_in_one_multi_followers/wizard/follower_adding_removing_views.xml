<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--
        This form view is designed for the `follower.adding.removing` model, allowing users
        to manage the addition or removal of followers and associated actions.

        Key Features:
        - The `type` field is displayed as a radio button widget, enabling the user to
          select the action type (e.g., adding or removing followers).
        - The `send_mail` field allows users to specify whether an email notification
          should be sent to the followers being added or removed.
        - The `partner_ids` field is displayed as a many2many_tags widget to select
          multiple followers (partners) for the action.
        - The `message` field becomes visible only when `send_mail` is checked, allowing
          users to compose a message that will be sent with the email notification.
        - The footer contains two buttons:
          - The "Submit" button triggers the `action_submit` method to apply the changes.
          - The "Cancel" button uses the `special="cancel"` attribute to discard changes
            and close the form.

        This form provides a simple interface for managing followers dynamically, with the
        option to notify them via email.
    -->
    <record id="follower_adding_removing_view_form" model="ir.ui.view">
        <field name="name">follower.adding.removing.view.form</field>
        <field name="model">follower.adding.removing</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="type" widget="radio"/>
                    <field name="send_mail"/>
                    <field name="partner_ids" widget="many2many_tags"/>
                    <field name="message" invisible="not send_mail"/>
                </group>
                <footer>
                    <button name="action_submit" string="Submit" type="object"
                            class="btn-primary"/>
                    <button string="Cancel" class="btn-default"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
