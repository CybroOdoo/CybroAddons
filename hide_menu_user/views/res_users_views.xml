<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--Inheriting fields to res.users-->
    <record id="view_users_form" model="ir.ui.view">
        <field name="name">res.users.view.form.inherit.hide.menu.user</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Hide Specific Menu" name="hide_specific_menu"
                      invisible="role == 'group_system' or is_show_specific_menu">
                    <field name="is_show_specific_menu" invisible="1"/>
                    <button name="%(action_hide_menu_copy_wizard)d" type="action"
                            string="Copy these to other users" class="btn-link"
                            icon="fa-copy" context="{'default_source_user_id': id}"/>
                    <field name="hide_menu_ids" nolabel="1">
                        <list>
                            <field name="complete_name" string="Menu"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
