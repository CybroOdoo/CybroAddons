<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ================================================================
         Inherit res.partner form — add churn risk smart button
         ================================================================ -->
    <record id="view_partner_form_churn_button" model="ir.ui.view">
        <field name="name">res.partner.form.churn.button</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button name="action_open_churn_score"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-heartbeat"
                        invisible="customer_rank == 0"
                        groups="crm_churn_predictor.group_churn_user">

                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_value">
                            <field name="churn_score_value"
                                   widget="float"
                                   digits="[6,1]"
                                   nolabel="1"/>
                        </span>
                        <span class="o_stat_text">
                            Churn:
                            <field name="churn_risk_level"
                                   nolabel="1"
                                   readonly="1"/>
                        </span>
                    </div>
                </button>
            </div>
        </field>
    </record>

</odoo>
