<?xml version="1.0" encoding="UTF-8" ?>
<!--
    Demo data for Blast Configuration

    This XML file creates a default demo record for the
    `blast.configuration` model. The record is marked as
    `noupdate="1"` to prevent it from being modified during
    module upgrades.
-->
<odoo>
    <data noupdate="1">
        <!--        Creating demo record in blast.configuration -->
        <record id="blast_configuration_data" model="blast.configuration">
            <field name="name">Blast Configuration</field>
        </record>
    </data>
</odoo>
