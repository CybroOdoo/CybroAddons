<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--    Server action to create BOM from product.product-->
    <record id="action_create_bom" model="ir.actions.server">
        <field name="name">Create BOM</field>
        <field name="model_id" ref="product.model_product_product"/>
        <field name="binding_model_id" ref="product.model_product_product"/>
        <field name="state">code</field>
        <field name="code">
            action = records.action_create_bom()
        </field>
    </record>

    <!--    Server action to create BOM from product.template-->
    <record id="action_create_bom_template" model="ir.actions.server">
        <field name="name">Create BOM</field>
        <field name="model_id" ref="product.model_product_template"/>
        <field name="binding_model_id" ref="product.model_product_template"/>
        <field name="state">code</field>
        <field name="code">
            action = records.action_create_bom()
        </field>
    </record>
</odoo>