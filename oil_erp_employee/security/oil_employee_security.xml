<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- This file defines the oil employee security rules and groups. -->
    <data noupdate="1">
        <record id="task_daily_expense_rule_company" model="ir.rule">
            <field name="name">Task Daily Expense: multi-company</field>
            <field name="model_id" ref="model_task_daily_expense"/>
            <field name="domain_force">['|', ('company_id', '=', False),
                ('company_id', 'in', company_ids)]
            </field>
        </record>
    </data>
</odoo>
