<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <!-- Inherit point_of_sale.Navbar View to Modify it -->
    <t t-name="Chrome" t-inherit="point_of_sale.Navbar"
       t-inherit-mode="extension" owl="1">
         <xpath expr="//div[hasclass('status-buttons')]" position="inside">
              <div class="ticket-button" id="pos_systray_chatter_icon"
                   t-att-class="{ highlight: props.isTicketScreenShown }"
                   t-on-click="onClick" style="cursor: pointer; position: relative;">
                <i class="fa fa-comments"/>
                <!-- Message count badge -->
                <t t-if="state.messageCount > 0">
                    <span style="position: absolute; top: -10px; right: 7px; background: #ff0000; color: white; border-radius: 50%; width: 18px; height: 18px; font-size: 10px; text-align: center; line-height: 18px;"
                          t-esc="state.messageCount"/>
                </t>
              </div>
        </xpath>
    </t>
</templates>