<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- This record defines the Suspicious Login attempt Rule -->
    <record id="suspicious_login_attempt_rule" model="ir.rule">
        <field name="name">Suspicious Login attempt Rule</field>
        <field name="model_id" ref="model_res_users_login_attempt"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
    </record>
</odoo>
