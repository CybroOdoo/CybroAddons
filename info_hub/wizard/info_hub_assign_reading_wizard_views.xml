<?xml version="1.0" encoding="utf-8"?>
<!--
    Form view and action for the 'Assign Article Reading' wizard, enabling assignment of mandatory reading to users.
-->
<odoo>
    <record id="info_assign_reading_wizard_view_form" model="ir.ui.view">
        <field name="name">info.hub.assign.reading.wizard.view.form</field>
        <field name="model">info.hub.assign.reading.wizard</field>
        <field name="arch" type="xml">
            <form string="Assign Mandatory Reading">
                <sheet>
                    <group>
                        <field name="article_id"/>
                        <field name="user_ids" widget="many2many_tags" placeholder="Select users..."/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_assign" string="Assign" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_info_assign_reading_wizard" model="ir.actions.act_window">
        <field name="name">Assign Mandatory Reading</field>
        <field name="res_model">info.hub.assign.reading.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="model_info_hub_article"/>
    </record>
</odoo>
