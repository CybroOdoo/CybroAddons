<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--    Inherit sale order to add a field-->
    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale.order.view.form.inherit.textile.management</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="is_textile_sale_order" invisible="1"/>
            </xpath>
            <xpath expr="//notebook/page[@name='other_information']" position="after">
                <page string="Customer Feedback" name="customer_feedback"
                      invisible="not website_id" groups="website.group_multi_website">
                    <group>
                        <field name="comment" string="Comment" widget="text"/>
                    </group>
                    <group>
                        <field name="rating" string="Rating"
                               widget="priority"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
