<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- This file defines the production wizard view. -->
    <record id="view_production_wizard_form" model="ir.ui.view">
        <field name="name">production.wizard.form</field>
        <field name="model">production.wizard</field>
        <field name="arch" type="xml">
            <form string="Record Production">
                <group>
                    <group>
                        <field name="task_id"/>
                        <field name="project_id"/>
                    </group>
                    <group>
                        <field name="production_date"/>
                        <field name="storage_location_id"/>
                    </group>
                </group>
                <notebook>
                    <page string="Production Lines" name="lines">
                        <field name="line_ids">
                            <list editable="bottom">
                                <field name="product_id"/>
                                <field name="uom_id"/>
                                <field name="produced_qty"/>
                                <field name="rate"/>
                            </list>
                        </field>
                    </page>
                </notebook>
                <footer>
                    <button name="action_confirm" string="Save Production"
                            type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_production_wizard" model="ir.actions.act_window">
        <field name="name">Record Production</field>
        <field name="res_model">production.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>