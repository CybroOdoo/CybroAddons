<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Defines access rules for Disciplinary Action:Employees can only view their own records.-->
    <record id="disciplinary_action_employee" model="ir.rule">
        <field name="name">Disciplinary Action Employee</field>
        <field ref="model_disciplinary_action" name="model_id"/>
        <field name="domain_force">[('employee_id.user_id.id','=',user.id)]
        </field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>
    <!-- Defines access rules for Disciplinary Action:HR Managers have full access to all records.-->
    <record id="disciplinary_action_manager" model="ir.rule">
        <field name="name">Disciplinary Action Manager</field>
        <field ref="model_disciplinary_action" name="model_id"/>
        <field name="domain_force">[]</field>
        <field name="groups" eval="[(4, ref('hr.group_hr_manager'))]"/>
    </record>
</odoo>
