<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Inherit the existing 'res.partner' tree view to add a button for importing images -->
    <record id="view_partner_tree" model="ir.ui.view">
        <field name="name">res.partner.view.tree.inherit.import.images.from.zip.file</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <list>
                <header>
                    <button name="action_open_import_image_wizard" string="Import image" type="object"
                            display="always"/>
                </header>
            </list>
        </field>
    </record>
</odoo>
