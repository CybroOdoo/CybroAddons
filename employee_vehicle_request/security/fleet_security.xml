<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
<!--        User can only see his/her vehicle-->
        <record id="fleet_rule_vehicle_visibility_user" model="ir.rule">
            <field name="name">User can only see his/her vehicle</field>
            <field name="model_id" ref="fleet.model_fleet_vehicle"/>
            <field name="groups" eval="[(Command.link(ref('base.group_user')))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
	        <field name="domain_force">[]</field>
        </record>
        <record id="employee_fleet_rule_user" model="ir.rule">
            <field name="name">Employee can only see their own requests</field>
            <field name="model_id" ref="model_employee_fleet"/>
            <field name="groups" eval="[(Command.link(ref('base.group_user')))]"/>
            <field name="domain_force">['|', ('employee_id.user_id', '=', user.id), ('employee_id.parent_id.user_id', '=', user.id)]</field>
        </record>
        <record id="employee_fleet_rule_hr_manager" model="ir.rule">
            <field name="name">HR Manager can see all requests</field>
            <field name="model_id" ref="model_employee_fleet"/>
            <field name="groups" eval="[(Command.link(ref('hr.group_hr_manager'))), (Command.link(ref('hr.group_hr_user')))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
        </record>
    </data>
</odoo>
