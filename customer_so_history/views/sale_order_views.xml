<?xml version="1.0" encoding="utf-8"?>
<odoo>
     <!-- Inherits Sale Order form to add sale order history -->
    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale.order.form.view.inherit.customer.so.history</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='order_lines']" position="after">
                <page string="Sale Order History">
                    <field name="order_details_ids">
                        <list editable="bottom" create="false" delete="false">
                            <field name="name"/>
                            <field name="product_id"/>
                            <field name="product_uom_qty"/>
                            <field name="company_id" invisible="1"/>
                            <field name="price_unit"/>
                            <field name="tax_id" widget="many2many_tags"/>
                            <field name="price_subtotal"/>
                            <button id="action_add_order_line" name="action_add"
                                    string="Add" class="btn-primary"
                                    type="object"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
