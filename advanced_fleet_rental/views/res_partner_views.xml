<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Action for All partners -->
    <record id="action_fleet_customer_menu" model="ir.actions.act_window">
        <field name="name">Partners</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">{'create': False, 'edit': False, 'delete':
            False}
        </field>

    </record>
    <!-- Menu item for Fleet Partners -->
    <menuitem id="menu_vehicle_rental_customers" name="Customers"
              parent="vehicle_management_root"
              action="action_fleet_customer_menu"
              sequence="4"/>
</odoo>