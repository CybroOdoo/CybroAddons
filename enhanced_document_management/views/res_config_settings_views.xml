<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Define a form view for the res.config.settings model -->
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.cyllo.documents
        </field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="15"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app string="Documents"
                     data-string="Documents"
                     name="document_management"
                     logo="/enhanced_document_management/static/description/icon.png">
                    <block title="Trash">
                        <setting id="trash" string="Trash Limits"
                                 help="This helps delete trash files automatically after the number of days you specify.">
                            <field name="trash"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>
    <!-- Define an action to open the Settings window with context -->
    <record id="document_config_settings_action" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_id" ref="res_config_settings_view_form"/>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module' : 'document_management'}</field>
    </record>
</odoo>
