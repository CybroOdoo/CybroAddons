<?xml version = "1.0" encoding = "UTF-8" ?>
<odoo>

    <!--   Inherit Ir Attachment form view to include extra fields-->
    <record id="view_attachment_form" model="ir.ui.view">
        <field name="name">ir.attachment.view.form.inherit.project.task.attachments</field>
        <field name="model">ir.attachment</field>
        <field name="inherit_id" ref="base.view_attachment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='public']" position="after">
                <field name="project_id" invisible="attach_to == 'task'"/>
                <field name="task_id" invisible="attach_to == 'project'"/>
            </xpath>
        </field>
    </record>

    <!--   Inherit Ir Attachment list view to include extra fields-->
    <record id="view_attachment_tree" model="ir.ui.view">
        <field name="name">ir.attachment.view.list.inherit.project.task.attachments</field>
        <field name="model">ir.attachment</field>
        <field name="inherit_id" ref="base.view_attachment_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='type']" position="after">
                <field name="project_id" optional="hide"/>
                <field name="task_id" optional="hide"/>
            </xpath>
        </field>
    </record>

    <!--   Inherit Ir Attachment list view to include extra fields-->
    <record id="view_document_file_kanban" model="ir.ui.view">
        <field name="name">ir.attachment.view.kanban.inherit.project.task.attachments</field>
        <field name="model">ir.attachment</field>
        <field name="inherit_id" ref="mail.view_document_file_kanban"/>
        <field name="arch" type="xml">
            <xpath expr="//kanban" position="inside">
                <field name="project_id"/>
                <field name="task_id"/>
            </xpath>
            <xpath expr="//main//field[@name='name']" position="after">
                <span t-if="record.project_id.value" class="d-flex">Project:
                    <field name="project_id"/>
                </span>
                <span t-if="record.task_id.value" class="d-flex">Task:
                    <field name="task_id"/>

                </span>
            </xpath>
        </field>
    </record>

    <!--   Project Documents menu action -->
    <record id="ir_attachment_action_view_task_documents" model="ir.actions.act_window">
        <field name="type">ir.actions.act_window</field>
        <field name="name">Task Documents</field>
        <field name="res_model">ir.attachment</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'default_attach_to': 'task','default_res_model': 'project.task'}</field>
        <field name="domain">[('res_model', '=', 'project.task')]</field>
    </record>

    <!--   Task Documents Menu Action -->
    <record id="ir_attachment_action_view_project_documents" model="ir.actions.act_window">
        <field name="type">ir.actions.act_window</field>
        <field name="name">Project Documents</field>
        <field name="res_model">ir.attachment</field>
        <field name="context">{'default_attach_to': 'project','default_res_model': 'project.project'}</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="domain">[('res_model', '=', 'project.project')]</field>
    </record>

    <!--   Documents Menu -->
    <menuitem id="ir_attachment_menu" name="Documents"
              parent="project.menu_main_pm"/>

    <!--   Project Documents Menu -->
    <menuitem id="ir_attachment_menu_view_project_documents"
              name="Project Documents"
              parent="ir_attachment_menu"
              action="ir_attachment_action_view_project_documents"/>
    <!--   Task Documents Menu -->
    <menuitem id="ir_attachment_menu_view_task_documents"
              name="Task Documents"
              parent="ir_attachment_menu"
              action="ir_attachment_action_view_task_documents"/>
</odoo>
