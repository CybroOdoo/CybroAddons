<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Record Rule: group_user can only see and filter their own media assets -->
        <record id="media_asset_rule_group_user" model="ir.rule">
            <field name="name">Media Asset: Users See Own Records</field>
            <field name="model_id" ref="model_media_asset"/>
            <field name="domain_force">[('create_uid', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('media_repository.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Record Rule: group_manager can see all media assets -->
        <record id="media_asset_rule_group_manager" model="ir.rule">
            <field name="name">Media Asset: Managers See All Records</field>
            <field name="model_id" ref="model_media_asset"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('media_repository.group_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>
